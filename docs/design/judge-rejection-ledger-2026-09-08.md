# Judge rejection ledger, 2026-09-07/08

Every finding every judge produced on the same input, verbatim from saved raw responses. Range judged: `da205c2..0f1035f` (the exact-tier cleanup lane as it was on main). Gate prompt: `.githooks/gate/provenance-gate-prompt.md` at 87dbc06. Temperature 0 on every gateway call. One row per finding per run; nothing summarized.

## Resolved model identities

| alias sent to gateway | gateway routes to | upstream response `model` field | source of that fact |
|---|---|---|---|
| gemini-subscription | openai/gemini-3.7-flash-high via cliproxy | (not captured; run predates response capture) | LiteLLM /model/info |
| claude-sub/claude-sonnet-5 | sub2api, passthrough | claude-sonnet-5 | sub2api container log, Anthropic response body |
| claude-sub/claude-opus-5 | sub2api, passthrough | claude-opus-5 | same |
| claude-sub/claude-fable-5-1 | sub2api, passthrough | claude-fable-5-1 | same |
| gpt-6-astra (earlier day) | cliproxy codex credential | gpt-6-astra | cliproxy log; quota-locked, resets 2026-09-14T02:38:43Z |

## Gate ledger lines for this range (`.git/decision-gate.log`)
```
2026-09-07T10:43:18Z range=da205c2..0f1035f diff=6aa276e84d55 judge=gateway model=gemini-subscription verdict=APPROVE
2026-09-07T10:44:51Z range=da205c2..0f1035f diff=6aa276e84d55 judge=gateway model=agy/gpt-6-astra verdict=REJECT
2026-09-07T10:51:26Z range=da205c2..0f1035f diff=6aa276e84d55 judge=gateway model=agy/gpt-6-astra verdict=REJECT
2026-09-07T11:00:13Z range=da205c2..0f1035f diff=6aa276e84d55 judge=gateway model=agy/gpt-6-astra verdict=REJECT
2026-09-07T11:01:35Z range=da205c2..0f1035f diff=6aa276e84d55 judge=gateway model=agy/gpt-6-astra verdict=REJECT
2026-09-07T11:01:50Z range=da205c2..0f1035f diff=6aa276e84d55 judge=gateway model=agy/gpt-6-astra verdict=REJECT
2026-09-07T16:08:07Z range=da205c2..0f1035f diff=6aa276e84d55 judge= model= verdict=REJECT
2026-09-07T16:08:12Z range=da205c2..0f1035f diff=6aa276e84d55 judge= model= verdict=REJECT
2026-09-07T16:08:12Z range=da205c2..0f1035f diff=6aa276e84d55 judge= model= verdict=REJECT
2026-09-07T16:08:27Z range=da205c2..0f1035f diff=6aa276e84d55 judge=gateway model=gemini-subscription verdict=APPROVE
2026-09-07T16:12:40Z range=da205c2..0f1035f diff=6aa276e84d55 judge= model= verdict=REJECT
2026-09-07T16:12:58Z range=da205c2..0f1035f diff=6aa276e84d55 judge= model= verdict=REJECT
2026-09-07T16:13:10Z range=da205c2..0f1035f diff=6aa276e84d55 judge=gateway model=claude-sub/claude-fable-5-1 verdict=REJECT
2026-09-07T17:47:03Z range=da205c2..0f1035f diff=6aa276e84d55 judge= model= verdict=REJECT
2026-09-07T17:47:20Z range=da205c2..0f1035f diff=6aa276e84d55 judge=gateway model=claude-sub/claude-opus-5 verdict=REJECT
2026-09-07T17:47:21Z range=da205c2..0f1035f diff=6aa276e84d55 judge=gateway model=claude-sub/claude-sonnet-5 verdict=REJECT
2026-09-07T21:54:54Z range=da205c2..0f1035f diff=6aa276e84d55 judge=gateway model=claude-sub/claude-fable-5-1 verdict=REJECT
```

## claude-sub/claude-sonnet-5  (10 runs, raw files: /tmp/judge-claude-sonnet-5/resp/)

### run 1 @ 01:46:53 local — verdict REJECT, 11 findings

| class | file:line | decision | reason / upstream_quote |
|---|---|---|---|
| FOUND | None:None | Compute per-list rank via SQL window function RANK() OVER (ORDER BY bm25_score) in a subquery, replacing Go-side enumeration | paradedb reciprocal_rank_fusion.sql#L32-60 uses the same window construct `RANK() OVER (ORDER BY score DESC) AS rank` |
| FOUND | None:None | bm25_score subquery ordered ascending (bm25() polarity) instead of DESC | language/engine scaffolding per doctrine's explicit carve-out for adapting paradedb's ORDER BY score DESC to SQLite's bm25() polarity; not a discretionary decision `ORDER BY pdb.score(order_id) DESC` |
| FOUND | None:None | RRF formula changed from 1.0/(k+listPosition+1) to 1.0/(k+SQLRank) using the SQL-computed rank | matches paradedb's fusion formula exactly `1.0 / (60 + rank) AS s` |
| FOUND | None:None | Tie-break changed from ISO string DESC to ID ascending in rrfAnchors/rrfHits sort comparators | paradedb final ORDER BY clause matches this exact tiebreak `ORDER BY score DESC, o.order_id` |
| MADE | None:None | New 'seen' map + list-order iteration to select which list's copy of a duplicate key (exact vs stemmed) survives dedup in rrfAnchors/rrfHits, replacing prior ma | No cited source states or implies that the exact-list's (first-argument) copy of a duplicate anchor/hit should win over the stemmed-list's copy; this ordering preference is invented composition of the two cited fusion sn  |
| MADE | None:None | New branch: when p.Sort is 'newest'/'oldest', skip RRF fusion entirely and instead merge exact+stemmed lists by dedup while preserving each list's original (SQL | meilisearch hybrid.rs#L51-57 compares ScoreValue::Sort vs ScoreValue::Score enum variants inside a ranking comparator -- it does not state 'skip fusion, merge by dedup preserving list order' -- and clickclack search_page  |
| FOUND | None:None | Removal of coverage/routine re-rank tie-break, replaced with sort purely by fused RRF score | coverage/routine logic is our own prior invention with no upstream citation ever attached to it, so its deletion is a 'loser deleted' under rule 4; the replacement (sort by descending score) is directly sourced `return results[i].Score > results[j].Score` |
| FOUND | None:None | Deletion of EscapeFTS5Query and its supporting regexes | deletion of our own previously-lifted-but-losing implementation, permitted as loser deletion under rule 4; no upstream requires it to remain  |
| POLICY | None:None | SettleWindow = 60s | already carries // policy: label citing docs/design/decision-references.md#D10  |
| POLICY | None:None | ROMmapSize = 0x7fff0000 | carries // policy: label citing docs/design/decision-references.md#D2  |
| FOUND | None:None | Addition of ID, BM25, Rank fields to SearchHit/SearchAnchor structs to carry SQL-computed rank/score through | plain struct wiring to plumb the cited RANK()/bm25() values through; not independent logic  |

### run 2 @ 01:46:58 local — verdict REJECT, 11 findings

| class | file:line | decision | reason / upstream_quote |
|---|---|---|---|
| FOUND | None:None | Compute per-list rank via SQL window function RANK() OVER (ORDER BY bm25_score) in a subquery, replacing Go-side enumeration | paradedb reciprocal_rank_fusion.sql#L32-60 uses the same window construct `RANK() OVER (ORDER BY score DESC) AS rank` |
| FOUND | None:None | bm25_score subquery ordered ascending (bm25() polarity) instead of DESC | language/engine scaffolding per doctrine's explicit carve-out for adapting paradedb's ORDER BY score DESC to SQLite's bm25() polarity; not a discretionary decision `ORDER BY pdb.score(order_id) DESC` |
| FOUND | None:None | RRF formula changed from 1.0/(k+listPosition+1) to 1.0/(k+SQLRank) using the SQL-computed rank | matches paradedb's fusion formula exactly `1.0 / (60 + rank) AS s` |
| FOUND | None:None | Tie-break changed from ISO string DESC to ID ascending in rrfAnchors/rrfHits sort comparators | paradedb final ORDER BY clause matches this exact tiebreak `ORDER BY score DESC, o.order_id` |
| MADE | None:None | New 'seen' map + list-order iteration to select which list's copy of a duplicate key (exact vs stemmed) survives dedup in rrfAnchors/rrfHits, replacing prior ma | No cited source states or implies that the exact-list's (first-argument) copy of a duplicate anchor/hit should win over the stemmed-list's copy; this ordering preference is invented composition of the two cited fusion sn  |
| MADE | None:None | New branch: when p.Sort is 'newest'/'oldest', skip RRF fusion entirely and instead merge exact+stemmed lists by dedup while preserving each list's original (SQL | meilisearch hybrid.rs#L51-57 compares ScoreValue::Sort vs ScoreValue::Score enum variants inside a ranking comparator -- it does not state 'skip fusion, merge by dedup preserving list order' -- and clickclack search_page  |
| FOUND | None:None | Removal of coverage/routine re-rank tie-break, replaced with sort purely by fused RRF score | coverage/routine logic is our own prior invention with no upstream citation ever attached to it, so its deletion is a 'loser deleted' under rule 4; the replacement (sort by descending score) is directly sourced `return results[i].Score > results[j].Score` |
| FOUND | None:None | Deletion of EscapeFTS5Query and its supporting regexes | deletion of our own previously-lifted-but-losing implementation, permitted as loser deletion under rule 4; no upstream requires it to remain  |
| POLICY | None:None | SettleWindow = 60s | already carries // policy: label citing docs/design/decision-references.md#D10  |
| POLICY | None:None | ROMmapSize = 0x7fff0000 | carries // policy: label citing docs/design/decision-references.md#D2  |
| FOUND | None:None | Addition of ID, BM25, Rank fields to SearchHit/SearchAnchor structs to carry SQL-computed rank/score through | plain struct wiring to plumb the cited RANK()/bm25() values through; not independent logic  |

### run 3 @ 01:47:01 local — verdict REJECT, 11 findings

| class | file:line | decision | reason / upstream_quote |
|---|---|---|---|
| FOUND | None:None | Compute per-list rank via SQL window function RANK() OVER (ORDER BY bm25_score) in a subquery, replacing Go-side enumeration | paradedb reciprocal_rank_fusion.sql#L32-60 uses the same window construct `RANK() OVER (ORDER BY score DESC) AS rank` |
| FOUND | None:None | bm25_score subquery ordered ascending (bm25() polarity) instead of DESC | language/engine scaffolding per doctrine's explicit carve-out for adapting paradedb's ORDER BY score DESC to SQLite's bm25() polarity; not a discretionary decision `ORDER BY pdb.score(order_id) DESC` |
| FOUND | None:None | RRF formula changed from 1.0/(k+listPosition+1) to 1.0/(k+SQLRank) using the SQL-computed rank | matches paradedb's fusion formula exactly `1.0 / (60 + rank) AS s` |
| FOUND | None:None | Tie-break changed from ISO string DESC to ID ascending in rrfAnchors/rrfHits sort comparators | paradedb final ORDER BY clause matches this exact tiebreak `ORDER BY score DESC, o.order_id` |
| MADE | None:None | New 'seen' map + list-order iteration to select which list's copy of a duplicate key (exact vs stemmed) survives dedup in rrfAnchors/rrfHits, replacing prior ma | No cited source states or implies that the exact-list's (first-argument) copy of a duplicate anchor/hit should win over the stemmed-list's copy; this ordering preference is invented composition of the two cited fusion sn  |
| MADE | None:None | New branch: when p.Sort is 'newest'/'oldest', skip RRF fusion entirely and instead merge exact+stemmed lists by dedup while preserving each list's original (SQL | meilisearch hybrid.rs#L51-57 compares ScoreValue::Sort vs ScoreValue::Score enum variants inside a ranking comparator -- it does not state 'skip fusion, merge by dedup preserving list order' -- and clickclack search_page  |
| FOUND | None:None | Removal of coverage/routine re-rank tie-break, replaced with sort purely by fused RRF score | coverage/routine logic is our own prior invention with no upstream citation ever attached to it, so its deletion is a 'loser deleted' under rule 4; the replacement (sort by descending score) is directly sourced `return results[i].Score > results[j].Score` |
| FOUND | None:None | Deletion of EscapeFTS5Query and its supporting regexes | deletion of our own previously-lifted-but-losing implementation, permitted as loser deletion under rule 4; no upstream requires it to remain  |
| POLICY | None:None | SettleWindow = 60s | already carries // policy: label citing docs/design/decision-references.md#D10  |
| POLICY | None:None | ROMmapSize = 0x7fff0000 | carries // policy: label citing docs/design/decision-references.md#D2  |
| FOUND | None:None | Addition of ID, BM25, Rank fields to SearchHit/SearchAnchor structs to carry SQL-computed rank/score through | plain struct wiring to plumb the cited RANK()/bm25() values through; not independent logic  |

### run 4 @ 01:47:04 local — verdict REJECT, 11 findings

| class | file:line | decision | reason / upstream_quote |
|---|---|---|---|
| FOUND | None:None | Compute per-list rank via SQL window function RANK() OVER (ORDER BY bm25_score) in a subquery, replacing Go-side enumeration | paradedb reciprocal_rank_fusion.sql#L32-60 uses the same window construct `RANK() OVER (ORDER BY score DESC) AS rank` |
| FOUND | None:None | bm25_score subquery ordered ascending (bm25() polarity) instead of DESC | language/engine scaffolding per doctrine's explicit carve-out for adapting paradedb's ORDER BY score DESC to SQLite's bm25() polarity; not a discretionary decision `ORDER BY pdb.score(order_id) DESC` |
| FOUND | None:None | RRF formula changed from 1.0/(k+listPosition+1) to 1.0/(k+SQLRank) using the SQL-computed rank | matches paradedb's fusion formula exactly `1.0 / (60 + rank) AS s` |
| FOUND | None:None | Tie-break changed from ISO string DESC to ID ascending in rrfAnchors/rrfHits sort comparators | paradedb final ORDER BY clause matches this exact tiebreak `ORDER BY score DESC, o.order_id` |
| MADE | None:None | New 'seen' map + list-order iteration to select which list's copy of a duplicate key (exact vs stemmed) survives dedup in rrfAnchors/rrfHits, replacing prior ma | No cited source states or implies that the exact-list's (first-argument) copy of a duplicate anchor/hit should win over the stemmed-list's copy; this ordering preference is invented composition of the two cited fusion sn  |
| MADE | None:None | New branch: when p.Sort is 'newest'/'oldest', skip RRF fusion entirely and instead merge exact+stemmed lists by dedup while preserving each list's original (SQL | meilisearch hybrid.rs#L51-57 compares ScoreValue::Sort vs ScoreValue::Score enum variants inside a ranking comparator -- it does not state 'skip fusion, merge by dedup preserving list order' -- and clickclack search_page  |
| FOUND | None:None | Removal of coverage/routine re-rank tie-break, replaced with sort purely by fused RRF score | coverage/routine logic is our own prior invention with no upstream citation ever attached to it, so its deletion is a 'loser deleted' under rule 4; the replacement (sort by descending score) is directly sourced `return results[i].Score > results[j].Score` |
| FOUND | None:None | Deletion of EscapeFTS5Query and its supporting regexes | deletion of our own previously-lifted-but-losing implementation, permitted as loser deletion under rule 4; no upstream requires it to remain  |
| POLICY | None:None | SettleWindow = 60s | already carries // policy: label citing docs/design/decision-references.md#D10  |
| POLICY | None:None | ROMmapSize = 0x7fff0000 | carries // policy: label citing docs/design/decision-references.md#D2  |
| FOUND | None:None | Addition of ID, BM25, Rank fields to SearchHit/SearchAnchor structs to carry SQL-computed rank/score through | plain struct wiring to plumb the cited RANK()/bm25() values through; not independent logic  |

### run 5 @ 01:47:07 local — verdict REJECT, 11 findings

| class | file:line | decision | reason / upstream_quote |
|---|---|---|---|
| FOUND | None:None | Compute per-list rank via SQL window function RANK() OVER (ORDER BY bm25_score) in a subquery, replacing Go-side enumeration | paradedb reciprocal_rank_fusion.sql#L32-60 uses the same window construct `RANK() OVER (ORDER BY score DESC) AS rank` |
| FOUND | None:None | bm25_score subquery ordered ascending (bm25() polarity) instead of DESC | language/engine scaffolding per doctrine's explicit carve-out for adapting paradedb's ORDER BY score DESC to SQLite's bm25() polarity; not a discretionary decision `ORDER BY pdb.score(order_id) DESC` |
| FOUND | None:None | RRF formula changed from 1.0/(k+listPosition+1) to 1.0/(k+SQLRank) using the SQL-computed rank | matches paradedb's fusion formula exactly `1.0 / (60 + rank) AS s` |
| FOUND | None:None | Tie-break changed from ISO string DESC to ID ascending in rrfAnchors/rrfHits sort comparators | paradedb final ORDER BY clause matches this exact tiebreak `ORDER BY score DESC, o.order_id` |
| MADE | None:None | New 'seen' map + list-order iteration to select which list's copy of a duplicate key (exact vs stemmed) survives dedup in rrfAnchors/rrfHits, replacing prior ma | No cited source states or implies that the exact-list's (first-argument) copy of a duplicate anchor/hit should win over the stemmed-list's copy; this ordering preference is invented composition of the two cited fusion sn  |
| MADE | None:None | New branch: when p.Sort is 'newest'/'oldest', skip RRF fusion entirely and instead merge exact+stemmed lists by dedup while preserving each list's original (SQL | meilisearch hybrid.rs#L51-57 compares ScoreValue::Sort vs ScoreValue::Score enum variants inside a ranking comparator -- it does not state 'skip fusion, merge by dedup preserving list order' -- and clickclack search_page  |
| FOUND | None:None | Removal of coverage/routine re-rank tie-break, replaced with sort purely by fused RRF score | coverage/routine logic is our own prior invention with no upstream citation ever attached to it, so its deletion is a 'loser deleted' under rule 4; the replacement (sort by descending score) is directly sourced `return results[i].Score > results[j].Score` |
| FOUND | None:None | Deletion of EscapeFTS5Query and its supporting regexes | deletion of our own previously-lifted-but-losing implementation, permitted as loser deletion under rule 4; no upstream requires it to remain  |
| POLICY | None:None | SettleWindow = 60s | already carries // policy: label citing docs/design/decision-references.md#D10  |
| POLICY | None:None | ROMmapSize = 0x7fff0000 | carries // policy: label citing docs/design/decision-references.md#D2  |
| FOUND | None:None | Addition of ID, BM25, Rank fields to SearchHit/SearchAnchor structs to carry SQL-computed rank/score through | plain struct wiring to plumb the cited RANK()/bm25() values through; not independent logic  |

### run 6 @ 01:47:10 local — verdict REJECT, 11 findings

| class | file:line | decision | reason / upstream_quote |
|---|---|---|---|
| FOUND | None:None | Compute per-list rank via SQL window function RANK() OVER (ORDER BY bm25_score) in a subquery, replacing Go-side enumeration | paradedb reciprocal_rank_fusion.sql#L32-60 uses the same window construct `RANK() OVER (ORDER BY score DESC) AS rank` |
| FOUND | None:None | bm25_score subquery ordered ascending (bm25() polarity) instead of DESC | language/engine scaffolding per doctrine's explicit carve-out for adapting paradedb's ORDER BY score DESC to SQLite's bm25() polarity; not a discretionary decision `ORDER BY pdb.score(order_id) DESC` |
| FOUND | None:None | RRF formula changed from 1.0/(k+listPosition+1) to 1.0/(k+SQLRank) using the SQL-computed rank | matches paradedb's fusion formula exactly `1.0 / (60 + rank) AS s` |
| FOUND | None:None | Tie-break changed from ISO string DESC to ID ascending in rrfAnchors/rrfHits sort comparators | paradedb final ORDER BY clause matches this exact tiebreak `ORDER BY score DESC, o.order_id` |
| MADE | None:None | New 'seen' map + list-order iteration to select which list's copy of a duplicate key (exact vs stemmed) survives dedup in rrfAnchors/rrfHits, replacing prior ma | No cited source states or implies that the exact-list's (first-argument) copy of a duplicate anchor/hit should win over the stemmed-list's copy; this ordering preference is invented composition of the two cited fusion sn  |
| MADE | None:None | New branch: when p.Sort is 'newest'/'oldest', skip RRF fusion entirely and instead merge exact+stemmed lists by dedup while preserving each list's original (SQL | meilisearch hybrid.rs#L51-57 compares ScoreValue::Sort vs ScoreValue::Score enum variants inside a ranking comparator -- it does not state 'skip fusion, merge by dedup preserving list order' -- and clickclack search_page  |
| FOUND | None:None | Removal of coverage/routine re-rank tie-break, replaced with sort purely by fused RRF score | coverage/routine logic is our own prior invention with no upstream citation ever attached to it, so its deletion is a 'loser deleted' under rule 4; the replacement (sort by descending score) is directly sourced `return results[i].Score > results[j].Score` |
| FOUND | None:None | Deletion of EscapeFTS5Query and its supporting regexes | deletion of our own previously-lifted-but-losing implementation, permitted as loser deletion under rule 4; no upstream requires it to remain  |
| POLICY | None:None | SettleWindow = 60s | already carries // policy: label citing docs/design/decision-references.md#D10  |
| POLICY | None:None | ROMmapSize = 0x7fff0000 | carries // policy: label citing docs/design/decision-references.md#D2  |
| FOUND | None:None | Addition of ID, BM25, Rank fields to SearchHit/SearchAnchor structs to carry SQL-computed rank/score through | plain struct wiring to plumb the cited RANK()/bm25() values through; not independent logic  |

### run 7 @ 01:47:12 local — verdict REJECT, 11 findings

| class | file:line | decision | reason / upstream_quote |
|---|---|---|---|
| FOUND | None:None | Compute per-list rank via SQL window function RANK() OVER (ORDER BY bm25_score) in a subquery, replacing Go-side enumeration | paradedb reciprocal_rank_fusion.sql#L32-60 uses the same window construct `RANK() OVER (ORDER BY score DESC) AS rank` |
| FOUND | None:None | bm25_score subquery ordered ascending (bm25() polarity) instead of DESC | language/engine scaffolding per doctrine's explicit carve-out for adapting paradedb's ORDER BY score DESC to SQLite's bm25() polarity; not a discretionary decision `ORDER BY pdb.score(order_id) DESC` |
| FOUND | None:None | RRF formula changed from 1.0/(k+listPosition+1) to 1.0/(k+SQLRank) using the SQL-computed rank | matches paradedb's fusion formula exactly `1.0 / (60 + rank) AS s` |
| FOUND | None:None | Tie-break changed from ISO string DESC to ID ascending in rrfAnchors/rrfHits sort comparators | paradedb final ORDER BY clause matches this exact tiebreak `ORDER BY score DESC, o.order_id` |
| MADE | None:None | New 'seen' map + list-order iteration to select which list's copy of a duplicate key (exact vs stemmed) survives dedup in rrfAnchors/rrfHits, replacing prior ma | No cited source states or implies that the exact-list's (first-argument) copy of a duplicate anchor/hit should win over the stemmed-list's copy; this ordering preference is invented composition of the two cited fusion sn  |
| MADE | None:None | New branch: when p.Sort is 'newest'/'oldest', skip RRF fusion entirely and instead merge exact+stemmed lists by dedup while preserving each list's original (SQL | meilisearch hybrid.rs#L51-57 compares ScoreValue::Sort vs ScoreValue::Score enum variants inside a ranking comparator -- it does not state 'skip fusion, merge by dedup preserving list order' -- and clickclack search_page  |
| FOUND | None:None | Removal of coverage/routine re-rank tie-break, replaced with sort purely by fused RRF score | coverage/routine logic is our own prior invention with no upstream citation ever attached to it, so its deletion is a 'loser deleted' under rule 4; the replacement (sort by descending score) is directly sourced `return results[i].Score > results[j].Score` |
| FOUND | None:None | Deletion of EscapeFTS5Query and its supporting regexes | deletion of our own previously-lifted-but-losing implementation, permitted as loser deletion under rule 4; no upstream requires it to remain  |
| POLICY | None:None | SettleWindow = 60s | already carries // policy: label citing docs/design/decision-references.md#D10  |
| POLICY | None:None | ROMmapSize = 0x7fff0000 | carries // policy: label citing docs/design/decision-references.md#D2  |
| FOUND | None:None | Addition of ID, BM25, Rank fields to SearchHit/SearchAnchor structs to carry SQL-computed rank/score through | plain struct wiring to plumb the cited RANK()/bm25() values through; not independent logic  |

### run 8 @ 01:47:16 local — verdict REJECT, 11 findings

| class | file:line | decision | reason / upstream_quote |
|---|---|---|---|
| FOUND | None:None | Compute per-list rank via SQL window function RANK() OVER (ORDER BY bm25_score) in a subquery, replacing Go-side enumeration | paradedb reciprocal_rank_fusion.sql#L32-60 uses the same window construct `RANK() OVER (ORDER BY score DESC) AS rank` |
| FOUND | None:None | bm25_score subquery ordered ascending (bm25() polarity) instead of DESC | language/engine scaffolding per doctrine's explicit carve-out for adapting paradedb's ORDER BY score DESC to SQLite's bm25() polarity; not a discretionary decision `ORDER BY pdb.score(order_id) DESC` |
| FOUND | None:None | RRF formula changed from 1.0/(k+listPosition+1) to 1.0/(k+SQLRank) using the SQL-computed rank | matches paradedb's fusion formula exactly `1.0 / (60 + rank) AS s` |
| FOUND | None:None | Tie-break changed from ISO string DESC to ID ascending in rrfAnchors/rrfHits sort comparators | paradedb final ORDER BY clause matches this exact tiebreak `ORDER BY score DESC, o.order_id` |
| MADE | None:None | New 'seen' map + list-order iteration to select which list's copy of a duplicate key (exact vs stemmed) survives dedup in rrfAnchors/rrfHits, replacing prior ma | No cited source states or implies that the exact-list's (first-argument) copy of a duplicate anchor/hit should win over the stemmed-list's copy; this ordering preference is invented composition of the two cited fusion sn  |
| MADE | None:None | New branch: when p.Sort is 'newest'/'oldest', skip RRF fusion entirely and instead merge exact+stemmed lists by dedup while preserving each list's original (SQL | meilisearch hybrid.rs#L51-57 compares ScoreValue::Sort vs ScoreValue::Score enum variants inside a ranking comparator -- it does not state 'skip fusion, merge by dedup preserving list order' -- and clickclack search_page  |
| FOUND | None:None | Removal of coverage/routine re-rank tie-break, replaced with sort purely by fused RRF score | coverage/routine logic is our own prior invention with no upstream citation ever attached to it, so its deletion is a 'loser deleted' under rule 4; the replacement (sort by descending score) is directly sourced `return results[i].Score > results[j].Score` |
| FOUND | None:None | Deletion of EscapeFTS5Query and its supporting regexes | deletion of our own previously-lifted-but-losing implementation, permitted as loser deletion under rule 4; no upstream requires it to remain  |
| POLICY | None:None | SettleWindow = 60s | already carries // policy: label citing docs/design/decision-references.md#D10  |
| POLICY | None:None | ROMmapSize = 0x7fff0000 | carries // policy: label citing docs/design/decision-references.md#D2  |
| FOUND | None:None | Addition of ID, BM25, Rank fields to SearchHit/SearchAnchor structs to carry SQL-computed rank/score through | plain struct wiring to plumb the cited RANK()/bm25() values through; not independent logic  |

### run 9 @ 01:47:18 local — verdict REJECT, 11 findings

| class | file:line | decision | reason / upstream_quote |
|---|---|---|---|
| FOUND | None:None | Compute per-list rank via SQL window function RANK() OVER (ORDER BY bm25_score) in a subquery, replacing Go-side enumeration | paradedb reciprocal_rank_fusion.sql#L32-60 uses the same window construct `RANK() OVER (ORDER BY score DESC) AS rank` |
| FOUND | None:None | bm25_score subquery ordered ascending (bm25() polarity) instead of DESC | language/engine scaffolding per doctrine's explicit carve-out for adapting paradedb's ORDER BY score DESC to SQLite's bm25() polarity; not a discretionary decision `ORDER BY pdb.score(order_id) DESC` |
| FOUND | None:None | RRF formula changed from 1.0/(k+listPosition+1) to 1.0/(k+SQLRank) using the SQL-computed rank | matches paradedb's fusion formula exactly `1.0 / (60 + rank) AS s` |
| FOUND | None:None | Tie-break changed from ISO string DESC to ID ascending in rrfAnchors/rrfHits sort comparators | paradedb final ORDER BY clause matches this exact tiebreak `ORDER BY score DESC, o.order_id` |
| MADE | None:None | New 'seen' map + list-order iteration to select which list's copy of a duplicate key (exact vs stemmed) survives dedup in rrfAnchors/rrfHits, replacing prior ma | No cited source states or implies that the exact-list's (first-argument) copy of a duplicate anchor/hit should win over the stemmed-list's copy; this ordering preference is invented composition of the two cited fusion sn  |
| MADE | None:None | New branch: when p.Sort is 'newest'/'oldest', skip RRF fusion entirely and instead merge exact+stemmed lists by dedup while preserving each list's original (SQL | meilisearch hybrid.rs#L51-57 compares ScoreValue::Sort vs ScoreValue::Score enum variants inside a ranking comparator -- it does not state 'skip fusion, merge by dedup preserving list order' -- and clickclack search_page  |
| FOUND | None:None | Removal of coverage/routine re-rank tie-break, replaced with sort purely by fused RRF score | coverage/routine logic is our own prior invention with no upstream citation ever attached to it, so its deletion is a 'loser deleted' under rule 4; the replacement (sort by descending score) is directly sourced `return results[i].Score > results[j].Score` |
| FOUND | None:None | Deletion of EscapeFTS5Query and its supporting regexes | deletion of our own previously-lifted-but-losing implementation, permitted as loser deletion under rule 4; no upstream requires it to remain  |
| POLICY | None:None | SettleWindow = 60s | already carries // policy: label citing docs/design/decision-references.md#D10  |
| POLICY | None:None | ROMmapSize = 0x7fff0000 | carries // policy: label citing docs/design/decision-references.md#D2  |
| FOUND | None:None | Addition of ID, BM25, Rank fields to SearchHit/SearchAnchor structs to carry SQL-computed rank/score through | plain struct wiring to plumb the cited RANK()/bm25() values through; not independent logic  |

### run 10 @ 01:47:21 local — verdict REJECT, 11 findings

| class | file:line | decision | reason / upstream_quote |
|---|---|---|---|
| FOUND | None:None | Compute per-list rank via SQL window function RANK() OVER (ORDER BY bm25_score) in a subquery, replacing Go-side enumeration | paradedb reciprocal_rank_fusion.sql#L32-60 uses the same window construct `RANK() OVER (ORDER BY score DESC) AS rank` |
| FOUND | None:None | bm25_score subquery ordered ascending (bm25() polarity) instead of DESC | language/engine scaffolding per doctrine's explicit carve-out for adapting paradedb's ORDER BY score DESC to SQLite's bm25() polarity; not a discretionary decision `ORDER BY pdb.score(order_id) DESC` |
| FOUND | None:None | RRF formula changed from 1.0/(k+listPosition+1) to 1.0/(k+SQLRank) using the SQL-computed rank | matches paradedb's fusion formula exactly `1.0 / (60 + rank) AS s` |
| FOUND | None:None | Tie-break changed from ISO string DESC to ID ascending in rrfAnchors/rrfHits sort comparators | paradedb final ORDER BY clause matches this exact tiebreak `ORDER BY score DESC, o.order_id` |
| MADE | None:None | New 'seen' map + list-order iteration to select which list's copy of a duplicate key (exact vs stemmed) survives dedup in rrfAnchors/rrfHits, replacing prior ma | No cited source states or implies that the exact-list's (first-argument) copy of a duplicate anchor/hit should win over the stemmed-list's copy; this ordering preference is invented composition of the two cited fusion sn  |
| MADE | None:None | New branch: when p.Sort is 'newest'/'oldest', skip RRF fusion entirely and instead merge exact+stemmed lists by dedup while preserving each list's original (SQL | meilisearch hybrid.rs#L51-57 compares ScoreValue::Sort vs ScoreValue::Score enum variants inside a ranking comparator -- it does not state 'skip fusion, merge by dedup preserving list order' -- and clickclack search_page  |
| FOUND | None:None | Removal of coverage/routine re-rank tie-break, replaced with sort purely by fused RRF score | coverage/routine logic is our own prior invention with no upstream citation ever attached to it, so its deletion is a 'loser deleted' under rule 4; the replacement (sort by descending score) is directly sourced `return results[i].Score > results[j].Score` |
| FOUND | None:None | Deletion of EscapeFTS5Query and its supporting regexes | deletion of our own previously-lifted-but-losing implementation, permitted as loser deletion under rule 4; no upstream requires it to remain  |
| POLICY | None:None | SettleWindow = 60s | already carries // policy: label citing docs/design/decision-references.md#D10  |
| POLICY | None:None | ROMmapSize = 0x7fff0000 | carries // policy: label citing docs/design/decision-references.md#D2  |
| FOUND | None:None | Addition of ID, BM25, Rank fields to SearchHit/SearchAnchor structs to carry SQL-computed rank/score through | plain struct wiring to plumb the cited RANK()/bm25() values through; not independent logic  |

## claude-sub/claude-opus-5  (10 runs, raw files: /tmp/judge-claude-opus-5/resp/)

### run 1 @ 01:46:51 local — verdict REJECT, 12 findings

| class | file:line | decision | reason / upstream_quote |
|---|---|---|---|
| POLICY | internal/index/consolidated.go:1464:None | SettleWindow = 60s freshness settle window | Labeled `// policy: 2026-09-04 60s settle see docs/design/decision-references.md#D10`; operational constant with date and design-doc reference.  |
| POLICY | internal/store/store.go:345:None | ROMmapSize = 0x7fff0000 read-only mmap window | Labeled `// policy: 2026-09-04 mmap 0x7fff0000 = SQLite ceiling see docs/design/decision-references.md#D2`; constant only, no control flow added.  |
| FOUND | internal/store/fts.go:224-245:None | Wrap the FTS5 query in a subquery and compute a window rank over the bm25 score (`RANK() OVER (ORDER BY bm25_score)`), with the inner query keeping LIMIT/OFFSET | paradedb reciprocal_rank_fusion.sql L32-L60 (trailer targets internal/store/fts.go) computes a window RANK over the relevance score of a limited inner query; the DESC->ASC flip is bm25() polarity adaptation (sqlite.org/f `RANK() OVER (ORDER BY score DESC) AS rank` |
| FOUND | internal/store/fts.go:238-243, 344-350:None | Inner query keeps its ORDER BY and LIMIT before ranking is applied. | paradedb L32-L60 (target internal/store/fts.go) orders and limits the inner select before the outer RANK. `ORDER BY pdb.score(order_id) DESC` |
| FOUND | internal/retrieve/retrieve.go:412, 462:None | RRF contribution computed as 1/(k + SQL rank) instead of 1/(k + positional index + 1). | paradedb reciprocal_rank_fusion.sql L49-L68 (trailer targets internal/retrieve/retrieve.go) fuses using the SQL window rank directly with no +1. `1.0 / (60 + rank) AS s FROM order_search` |
| FOUND | internal/retrieve/retrieve.go:444, 485:None | Tie-break after equal fused score is ascending row ID (replacing ISO-desc / SessionID-asc tie-breaks). | paradedb L49-L68 (target internal/retrieve/retrieve.go) orders by score descending then by the row identifier ascending; the removed ISO/SessionID tie-breaks were local inventions, deleted under doctrine rule 4. `ORDER BY score DESC, o.order_id` |
| FOUND | internal/retrieve/retrieve.go:449, 490:None | Truncate fused results to the requested limit when limit > 0. | grepai search/hybrid.go L57-L89 (target internal/retrieve/retrieve.go) truncates identically. `if limit > 0 && len(results) > limit` |
| FOUND | internal/retrieve/retrieve.go:610-616, 917-921:None | Relevance-mode re-rank sorts solely by descending fused RRF score (coverage and routine partition removed). | grepai search/hybrid.go L57-L89 (target internal/retrieve/retrieve.go) sorts fused results by descending score; the removed coverage/routine heuristic was local, deletable under doctrine rule 4. `return results[i].Score > results[j].Score` |
| MADE | internal/retrieve/retrieve.go:553-570:None | Branch `if p.Sort == "newest" \|\| p.Sort == "oldest"` bypasses RRF fusion entirely and instead concatenates exact+stemmed hit lists, dedups them, and truncates t | No cited source contains this branch. meilisearch hybrid.rs L51-L57 is a comparator match arm on ScoreValue::Sort; it contains no branch that skips fusion. clickclack search_pages.go L56-L75 builds sort-dependent cursor   |
| MADE | internal/retrieve/retrieve.go:851-871:None | Same newest/oldest fusion-bypass branch with dedup-preserving-SQL-order merge in MatchAnchors. | Same as above: neither the meilisearch nor the clickclack fetched lines contain a branch that replaces RRF with an ordered dedup merge for time-sort modes.  |
| MADE | internal/retrieve/retrieve.go:592-595, 893-899:None | Nil-map guard `if hitScores != nil` / `if anchorScores != nil` before reading the fused score, defaulting fused to 0. | Added conditional fallback; grepai always reads from a populated score map and has no such guard. A defensive condition the source lacks is LOGIC under doctrine ('What wiring means, strictly').  |
| MADE | internal/retrieve/retrieve.go:893-896:None | Key derivation fallback in MatchAnchors: use a.UUID, else SessionID + ":" + ISO. | grepai keys results by a single chunk ID (`id := result.Chunk.ID`); the empty-UUID branch and the string concatenation of two fields have no cited upstream line (RubyHeron 359 case B).  |

### run 2 @ 01:46:55 local — verdict REJECT, 12 findings

| class | file:line | decision | reason / upstream_quote |
|---|---|---|---|
| POLICY | internal/index/consolidated.go:1464:None | SettleWindow = 60s freshness settle window | Labeled `// policy: 2026-09-04 60s settle see docs/design/decision-references.md#D10`; operational constant with date and design-doc reference.  |
| POLICY | internal/store/store.go:345:None | ROMmapSize = 0x7fff0000 read-only mmap window | Labeled `// policy: 2026-09-04 mmap 0x7fff0000 = SQLite ceiling see docs/design/decision-references.md#D2`; constant only, no control flow added.  |
| FOUND | internal/store/fts.go:224-245:None | Wrap the FTS5 query in a subquery and compute a window rank over the bm25 score (`RANK() OVER (ORDER BY bm25_score)`), with the inner query keeping LIMIT/OFFSET | paradedb reciprocal_rank_fusion.sql L32-L60 (trailer targets internal/store/fts.go) computes a window RANK over the relevance score of a limited inner query; the DESC->ASC flip is bm25() polarity adaptation (sqlite.org/f `RANK() OVER (ORDER BY score DESC) AS rank` |
| FOUND | internal/store/fts.go:238-243, 344-350:None | Inner query keeps its ORDER BY and LIMIT before ranking is applied. | paradedb L32-L60 (target internal/store/fts.go) orders and limits the inner select before the outer RANK. `ORDER BY pdb.score(order_id) DESC` |
| FOUND | internal/retrieve/retrieve.go:412, 462:None | RRF contribution computed as 1/(k + SQL rank) instead of 1/(k + positional index + 1). | paradedb reciprocal_rank_fusion.sql L49-L68 (trailer targets internal/retrieve/retrieve.go) fuses using the SQL window rank directly with no +1. `1.0 / (60 + rank) AS s FROM order_search` |
| FOUND | internal/retrieve/retrieve.go:444, 485:None | Tie-break after equal fused score is ascending row ID (replacing ISO-desc / SessionID-asc tie-breaks). | paradedb L49-L68 (target internal/retrieve/retrieve.go) orders by score descending then by the row identifier ascending; the removed ISO/SessionID tie-breaks were local inventions, deleted under doctrine rule 4. `ORDER BY score DESC, o.order_id` |
| FOUND | internal/retrieve/retrieve.go:449, 490:None | Truncate fused results to the requested limit when limit > 0. | grepai search/hybrid.go L57-L89 (target internal/retrieve/retrieve.go) truncates identically. `if limit > 0 && len(results) > limit` |
| FOUND | internal/retrieve/retrieve.go:610-616, 917-921:None | Relevance-mode re-rank sorts solely by descending fused RRF score (coverage and routine partition removed). | grepai search/hybrid.go L57-L89 (target internal/retrieve/retrieve.go) sorts fused results by descending score; the removed coverage/routine heuristic was local, deletable under doctrine rule 4. `return results[i].Score > results[j].Score` |
| MADE | internal/retrieve/retrieve.go:553-570:None | Branch `if p.Sort == "newest" \|\| p.Sort == "oldest"` bypasses RRF fusion entirely and instead concatenates exact+stemmed hit lists, dedups them, and truncates t | No cited source contains this branch. meilisearch hybrid.rs L51-L57 is a comparator match arm on ScoreValue::Sort; it contains no branch that skips fusion. clickclack search_pages.go L56-L75 builds sort-dependent cursor   |
| MADE | internal/retrieve/retrieve.go:851-871:None | Same newest/oldest fusion-bypass branch with dedup-preserving-SQL-order merge in MatchAnchors. | Same as above: neither the meilisearch nor the clickclack fetched lines contain a branch that replaces RRF with an ordered dedup merge for time-sort modes.  |
| MADE | internal/retrieve/retrieve.go:592-595, 893-899:None | Nil-map guard `if hitScores != nil` / `if anchorScores != nil` before reading the fused score, defaulting fused to 0. | Added conditional fallback; grepai always reads from a populated score map and has no such guard. A defensive condition the source lacks is LOGIC under doctrine ('What wiring means, strictly').  |
| MADE | internal/retrieve/retrieve.go:893-896:None | Key derivation fallback in MatchAnchors: use a.UUID, else SessionID + ":" + ISO. | grepai keys results by a single chunk ID (`id := result.Chunk.ID`); the empty-UUID branch and the string concatenation of two fields have no cited upstream line (RubyHeron 359 case B).  |

### run 3 @ 01:46:58 local — verdict REJECT, 12 findings

| class | file:line | decision | reason / upstream_quote |
|---|---|---|---|
| POLICY | internal/index/consolidated.go:1464:None | SettleWindow = 60s freshness settle window | Labeled `// policy: 2026-09-04 60s settle see docs/design/decision-references.md#D10`; operational constant with date and design-doc reference.  |
| POLICY | internal/store/store.go:345:None | ROMmapSize = 0x7fff0000 read-only mmap window | Labeled `// policy: 2026-09-04 mmap 0x7fff0000 = SQLite ceiling see docs/design/decision-references.md#D2`; constant only, no control flow added.  |
| FOUND | internal/store/fts.go:224-245:None | Wrap the FTS5 query in a subquery and compute a window rank over the bm25 score (`RANK() OVER (ORDER BY bm25_score)`), with the inner query keeping LIMIT/OFFSET | paradedb reciprocal_rank_fusion.sql L32-L60 (trailer targets internal/store/fts.go) computes a window RANK over the relevance score of a limited inner query; the DESC->ASC flip is bm25() polarity adaptation (sqlite.org/f `RANK() OVER (ORDER BY score DESC) AS rank` |
| FOUND | internal/store/fts.go:238-243, 344-350:None | Inner query keeps its ORDER BY and LIMIT before ranking is applied. | paradedb L32-L60 (target internal/store/fts.go) orders and limits the inner select before the outer RANK. `ORDER BY pdb.score(order_id) DESC` |
| FOUND | internal/retrieve/retrieve.go:412, 462:None | RRF contribution computed as 1/(k + SQL rank) instead of 1/(k + positional index + 1). | paradedb reciprocal_rank_fusion.sql L49-L68 (trailer targets internal/retrieve/retrieve.go) fuses using the SQL window rank directly with no +1. `1.0 / (60 + rank) AS s FROM order_search` |
| FOUND | internal/retrieve/retrieve.go:444, 485:None | Tie-break after equal fused score is ascending row ID (replacing ISO-desc / SessionID-asc tie-breaks). | paradedb L49-L68 (target internal/retrieve/retrieve.go) orders by score descending then by the row identifier ascending; the removed ISO/SessionID tie-breaks were local inventions, deleted under doctrine rule 4. `ORDER BY score DESC, o.order_id` |
| FOUND | internal/retrieve/retrieve.go:449, 490:None | Truncate fused results to the requested limit when limit > 0. | grepai search/hybrid.go L57-L89 (target internal/retrieve/retrieve.go) truncates identically. `if limit > 0 && len(results) > limit` |
| FOUND | internal/retrieve/retrieve.go:610-616, 917-921:None | Relevance-mode re-rank sorts solely by descending fused RRF score (coverage and routine partition removed). | grepai search/hybrid.go L57-L89 (target internal/retrieve/retrieve.go) sorts fused results by descending score; the removed coverage/routine heuristic was local, deletable under doctrine rule 4. `return results[i].Score > results[j].Score` |
| MADE | internal/retrieve/retrieve.go:553-570:None | Branch `if p.Sort == "newest" \|\| p.Sort == "oldest"` bypasses RRF fusion entirely and instead concatenates exact+stemmed hit lists, dedups them, and truncates t | No cited source contains this branch. meilisearch hybrid.rs L51-L57 is a comparator match arm on ScoreValue::Sort; it contains no branch that skips fusion. clickclack search_pages.go L56-L75 builds sort-dependent cursor   |
| MADE | internal/retrieve/retrieve.go:851-871:None | Same newest/oldest fusion-bypass branch with dedup-preserving-SQL-order merge in MatchAnchors. | Same as above: neither the meilisearch nor the clickclack fetched lines contain a branch that replaces RRF with an ordered dedup merge for time-sort modes.  |
| MADE | internal/retrieve/retrieve.go:592-595, 893-899:None | Nil-map guard `if hitScores != nil` / `if anchorScores != nil` before reading the fused score, defaulting fused to 0. | Added conditional fallback; grepai always reads from a populated score map and has no such guard. A defensive condition the source lacks is LOGIC under doctrine ('What wiring means, strictly').  |
| MADE | internal/retrieve/retrieve.go:893-896:None | Key derivation fallback in MatchAnchors: use a.UUID, else SessionID + ":" + ISO. | grepai keys results by a single chunk ID (`id := result.Chunk.ID`); the empty-UUID branch and the string concatenation of two fields have no cited upstream line (RubyHeron 359 case B).  |

### run 4 @ 01:47:01 local — verdict REJECT, 12 findings

| class | file:line | decision | reason / upstream_quote |
|---|---|---|---|
| POLICY | internal/index/consolidated.go:1464:None | SettleWindow = 60s freshness settle window | Labeled `// policy: 2026-09-04 60s settle see docs/design/decision-references.md#D10`; operational constant with date and design-doc reference.  |
| POLICY | internal/store/store.go:345:None | ROMmapSize = 0x7fff0000 read-only mmap window | Labeled `// policy: 2026-09-04 mmap 0x7fff0000 = SQLite ceiling see docs/design/decision-references.md#D2`; constant only, no control flow added.  |
| FOUND | internal/store/fts.go:224-245:None | Wrap the FTS5 query in a subquery and compute a window rank over the bm25 score (`RANK() OVER (ORDER BY bm25_score)`), with the inner query keeping LIMIT/OFFSET | paradedb reciprocal_rank_fusion.sql L32-L60 (trailer targets internal/store/fts.go) computes a window RANK over the relevance score of a limited inner query; the DESC->ASC flip is bm25() polarity adaptation (sqlite.org/f `RANK() OVER (ORDER BY score DESC) AS rank` |
| FOUND | internal/store/fts.go:238-243, 344-350:None | Inner query keeps its ORDER BY and LIMIT before ranking is applied. | paradedb L32-L60 (target internal/store/fts.go) orders and limits the inner select before the outer RANK. `ORDER BY pdb.score(order_id) DESC` |
| FOUND | internal/retrieve/retrieve.go:412, 462:None | RRF contribution computed as 1/(k + SQL rank) instead of 1/(k + positional index + 1). | paradedb reciprocal_rank_fusion.sql L49-L68 (trailer targets internal/retrieve/retrieve.go) fuses using the SQL window rank directly with no +1. `1.0 / (60 + rank) AS s FROM order_search` |
| FOUND | internal/retrieve/retrieve.go:444, 485:None | Tie-break after equal fused score is ascending row ID (replacing ISO-desc / SessionID-asc tie-breaks). | paradedb L49-L68 (target internal/retrieve/retrieve.go) orders by score descending then by the row identifier ascending; the removed ISO/SessionID tie-breaks were local inventions, deleted under doctrine rule 4. `ORDER BY score DESC, o.order_id` |
| FOUND | internal/retrieve/retrieve.go:449, 490:None | Truncate fused results to the requested limit when limit > 0. | grepai search/hybrid.go L57-L89 (target internal/retrieve/retrieve.go) truncates identically. `if limit > 0 && len(results) > limit` |
| FOUND | internal/retrieve/retrieve.go:610-616, 917-921:None | Relevance-mode re-rank sorts solely by descending fused RRF score (coverage and routine partition removed). | grepai search/hybrid.go L57-L89 (target internal/retrieve/retrieve.go) sorts fused results by descending score; the removed coverage/routine heuristic was local, deletable under doctrine rule 4. `return results[i].Score > results[j].Score` |
| MADE | internal/retrieve/retrieve.go:553-570:None | Branch `if p.Sort == "newest" \|\| p.Sort == "oldest"` bypasses RRF fusion entirely and instead concatenates exact+stemmed hit lists, dedups them, and truncates t | No cited source contains this branch. meilisearch hybrid.rs L51-L57 is a comparator match arm on ScoreValue::Sort; it contains no branch that skips fusion. clickclack search_pages.go L56-L75 builds sort-dependent cursor   |
| MADE | internal/retrieve/retrieve.go:851-871:None | Same newest/oldest fusion-bypass branch with dedup-preserving-SQL-order merge in MatchAnchors. | Same as above: neither the meilisearch nor the clickclack fetched lines contain a branch that replaces RRF with an ordered dedup merge for time-sort modes.  |
| MADE | internal/retrieve/retrieve.go:592-595, 893-899:None | Nil-map guard `if hitScores != nil` / `if anchorScores != nil` before reading the fused score, defaulting fused to 0. | Added conditional fallback; grepai always reads from a populated score map and has no such guard. A defensive condition the source lacks is LOGIC under doctrine ('What wiring means, strictly').  |
| MADE | internal/retrieve/retrieve.go:893-896:None | Key derivation fallback in MatchAnchors: use a.UUID, else SessionID + ":" + ISO. | grepai keys results by a single chunk ID (`id := result.Chunk.ID`); the empty-UUID branch and the string concatenation of two fields have no cited upstream line (RubyHeron 359 case B).  |

### run 5 @ 01:47:05 local — verdict REJECT, 12 findings

| class | file:line | decision | reason / upstream_quote |
|---|---|---|---|
| POLICY | internal/index/consolidated.go:1464:None | SettleWindow = 60s freshness settle window | Labeled `// policy: 2026-09-04 60s settle see docs/design/decision-references.md#D10`; operational constant with date and design-doc reference.  |
| POLICY | internal/store/store.go:345:None | ROMmapSize = 0x7fff0000 read-only mmap window | Labeled `// policy: 2026-09-04 mmap 0x7fff0000 = SQLite ceiling see docs/design/decision-references.md#D2`; constant only, no control flow added.  |
| FOUND | internal/store/fts.go:224-245:None | Wrap the FTS5 query in a subquery and compute a window rank over the bm25 score (`RANK() OVER (ORDER BY bm25_score)`), with the inner query keeping LIMIT/OFFSET | paradedb reciprocal_rank_fusion.sql L32-L60 (trailer targets internal/store/fts.go) computes a window RANK over the relevance score of a limited inner query; the DESC->ASC flip is bm25() polarity adaptation (sqlite.org/f `RANK() OVER (ORDER BY score DESC) AS rank` |
| FOUND | internal/store/fts.go:238-243, 344-350:None | Inner query keeps its ORDER BY and LIMIT before ranking is applied. | paradedb L32-L60 (target internal/store/fts.go) orders and limits the inner select before the outer RANK. `ORDER BY pdb.score(order_id) DESC` |
| FOUND | internal/retrieve/retrieve.go:412, 462:None | RRF contribution computed as 1/(k + SQL rank) instead of 1/(k + positional index + 1). | paradedb reciprocal_rank_fusion.sql L49-L68 (trailer targets internal/retrieve/retrieve.go) fuses using the SQL window rank directly with no +1. `1.0 / (60 + rank) AS s FROM order_search` |
| FOUND | internal/retrieve/retrieve.go:444, 485:None | Tie-break after equal fused score is ascending row ID (replacing ISO-desc / SessionID-asc tie-breaks). | paradedb L49-L68 (target internal/retrieve/retrieve.go) orders by score descending then by the row identifier ascending; the removed ISO/SessionID tie-breaks were local inventions, deleted under doctrine rule 4. `ORDER BY score DESC, o.order_id` |
| FOUND | internal/retrieve/retrieve.go:449, 490:None | Truncate fused results to the requested limit when limit > 0. | grepai search/hybrid.go L57-L89 (target internal/retrieve/retrieve.go) truncates identically. `if limit > 0 && len(results) > limit` |
| FOUND | internal/retrieve/retrieve.go:610-616, 917-921:None | Relevance-mode re-rank sorts solely by descending fused RRF score (coverage and routine partition removed). | grepai search/hybrid.go L57-L89 (target internal/retrieve/retrieve.go) sorts fused results by descending score; the removed coverage/routine heuristic was local, deletable under doctrine rule 4. `return results[i].Score > results[j].Score` |
| MADE | internal/retrieve/retrieve.go:553-570:None | Branch `if p.Sort == "newest" \|\| p.Sort == "oldest"` bypasses RRF fusion entirely and instead concatenates exact+stemmed hit lists, dedups them, and truncates t | No cited source contains this branch. meilisearch hybrid.rs L51-L57 is a comparator match arm on ScoreValue::Sort; it contains no branch that skips fusion. clickclack search_pages.go L56-L75 builds sort-dependent cursor   |
| MADE | internal/retrieve/retrieve.go:851-871:None | Same newest/oldest fusion-bypass branch with dedup-preserving-SQL-order merge in MatchAnchors. | Same as above: neither the meilisearch nor the clickclack fetched lines contain a branch that replaces RRF with an ordered dedup merge for time-sort modes.  |
| MADE | internal/retrieve/retrieve.go:592-595, 893-899:None | Nil-map guard `if hitScores != nil` / `if anchorScores != nil` before reading the fused score, defaulting fused to 0. | Added conditional fallback; grepai always reads from a populated score map and has no such guard. A defensive condition the source lacks is LOGIC under doctrine ('What wiring means, strictly').  |
| MADE | internal/retrieve/retrieve.go:893-896:None | Key derivation fallback in MatchAnchors: use a.UUID, else SessionID + ":" + ISO. | grepai keys results by a single chunk ID (`id := result.Chunk.ID`); the empty-UUID branch and the string concatenation of two fields have no cited upstream line (RubyHeron 359 case B).  |

### run 6 @ 01:47:07 local — verdict REJECT, 12 findings

| class | file:line | decision | reason / upstream_quote |
|---|---|---|---|
| POLICY | internal/index/consolidated.go:1464:None | SettleWindow = 60s freshness settle window | Labeled `// policy: 2026-09-04 60s settle see docs/design/decision-references.md#D10`; operational constant with date and design-doc reference.  |
| POLICY | internal/store/store.go:345:None | ROMmapSize = 0x7fff0000 read-only mmap window | Labeled `// policy: 2026-09-04 mmap 0x7fff0000 = SQLite ceiling see docs/design/decision-references.md#D2`; constant only, no control flow added.  |
| FOUND | internal/store/fts.go:224-245:None | Wrap the FTS5 query in a subquery and compute a window rank over the bm25 score (`RANK() OVER (ORDER BY bm25_score)`), with the inner query keeping LIMIT/OFFSET | paradedb reciprocal_rank_fusion.sql L32-L60 (trailer targets internal/store/fts.go) computes a window RANK over the relevance score of a limited inner query; the DESC->ASC flip is bm25() polarity adaptation (sqlite.org/f `RANK() OVER (ORDER BY score DESC) AS rank` |
| FOUND | internal/store/fts.go:238-243, 344-350:None | Inner query keeps its ORDER BY and LIMIT before ranking is applied. | paradedb L32-L60 (target internal/store/fts.go) orders and limits the inner select before the outer RANK. `ORDER BY pdb.score(order_id) DESC` |
| FOUND | internal/retrieve/retrieve.go:412, 462:None | RRF contribution computed as 1/(k + SQL rank) instead of 1/(k + positional index + 1). | paradedb reciprocal_rank_fusion.sql L49-L68 (trailer targets internal/retrieve/retrieve.go) fuses using the SQL window rank directly with no +1. `1.0 / (60 + rank) AS s FROM order_search` |
| FOUND | internal/retrieve/retrieve.go:444, 485:None | Tie-break after equal fused score is ascending row ID (replacing ISO-desc / SessionID-asc tie-breaks). | paradedb L49-L68 (target internal/retrieve/retrieve.go) orders by score descending then by the row identifier ascending; the removed ISO/SessionID tie-breaks were local inventions, deleted under doctrine rule 4. `ORDER BY score DESC, o.order_id` |
| FOUND | internal/retrieve/retrieve.go:449, 490:None | Truncate fused results to the requested limit when limit > 0. | grepai search/hybrid.go L57-L89 (target internal/retrieve/retrieve.go) truncates identically. `if limit > 0 && len(results) > limit` |
| FOUND | internal/retrieve/retrieve.go:610-616, 917-921:None | Relevance-mode re-rank sorts solely by descending fused RRF score (coverage and routine partition removed). | grepai search/hybrid.go L57-L89 (target internal/retrieve/retrieve.go) sorts fused results by descending score; the removed coverage/routine heuristic was local, deletable under doctrine rule 4. `return results[i].Score > results[j].Score` |
| MADE | internal/retrieve/retrieve.go:553-570:None | Branch `if p.Sort == "newest" \|\| p.Sort == "oldest"` bypasses RRF fusion entirely and instead concatenates exact+stemmed hit lists, dedups them, and truncates t | No cited source contains this branch. meilisearch hybrid.rs L51-L57 is a comparator match arm on ScoreValue::Sort; it contains no branch that skips fusion. clickclack search_pages.go L56-L75 builds sort-dependent cursor   |
| MADE | internal/retrieve/retrieve.go:851-871:None | Same newest/oldest fusion-bypass branch with dedup-preserving-SQL-order merge in MatchAnchors. | Same as above: neither the meilisearch nor the clickclack fetched lines contain a branch that replaces RRF with an ordered dedup merge for time-sort modes.  |
| MADE | internal/retrieve/retrieve.go:592-595, 893-899:None | Nil-map guard `if hitScores != nil` / `if anchorScores != nil` before reading the fused score, defaulting fused to 0. | Added conditional fallback; grepai always reads from a populated score map and has no such guard. A defensive condition the source lacks is LOGIC under doctrine ('What wiring means, strictly').  |
| MADE | internal/retrieve/retrieve.go:893-896:None | Key derivation fallback in MatchAnchors: use a.UUID, else SessionID + ":" + ISO. | grepai keys results by a single chunk ID (`id := result.Chunk.ID`); the empty-UUID branch and the string concatenation of two fields have no cited upstream line (RubyHeron 359 case B).  |

### run 7 @ 01:47:10 local — verdict REJECT, 12 findings

| class | file:line | decision | reason / upstream_quote |
|---|---|---|---|
| POLICY | internal/index/consolidated.go:1464:None | SettleWindow = 60s freshness settle window | Labeled `// policy: 2026-09-04 60s settle see docs/design/decision-references.md#D10`; operational constant with date and design-doc reference.  |
| POLICY | internal/store/store.go:345:None | ROMmapSize = 0x7fff0000 read-only mmap window | Labeled `// policy: 2026-09-04 mmap 0x7fff0000 = SQLite ceiling see docs/design/decision-references.md#D2`; constant only, no control flow added.  |
| FOUND | internal/store/fts.go:224-245:None | Wrap the FTS5 query in a subquery and compute a window rank over the bm25 score (`RANK() OVER (ORDER BY bm25_score)`), with the inner query keeping LIMIT/OFFSET | paradedb reciprocal_rank_fusion.sql L32-L60 (trailer targets internal/store/fts.go) computes a window RANK over the relevance score of a limited inner query; the DESC->ASC flip is bm25() polarity adaptation (sqlite.org/f `RANK() OVER (ORDER BY score DESC) AS rank` |
| FOUND | internal/store/fts.go:238-243, 344-350:None | Inner query keeps its ORDER BY and LIMIT before ranking is applied. | paradedb L32-L60 (target internal/store/fts.go) orders and limits the inner select before the outer RANK. `ORDER BY pdb.score(order_id) DESC` |
| FOUND | internal/retrieve/retrieve.go:412, 462:None | RRF contribution computed as 1/(k + SQL rank) instead of 1/(k + positional index + 1). | paradedb reciprocal_rank_fusion.sql L49-L68 (trailer targets internal/retrieve/retrieve.go) fuses using the SQL window rank directly with no +1. `1.0 / (60 + rank) AS s FROM order_search` |
| FOUND | internal/retrieve/retrieve.go:444, 485:None | Tie-break after equal fused score is ascending row ID (replacing ISO-desc / SessionID-asc tie-breaks). | paradedb L49-L68 (target internal/retrieve/retrieve.go) orders by score descending then by the row identifier ascending; the removed ISO/SessionID tie-breaks were local inventions, deleted under doctrine rule 4. `ORDER BY score DESC, o.order_id` |
| FOUND | internal/retrieve/retrieve.go:449, 490:None | Truncate fused results to the requested limit when limit > 0. | grepai search/hybrid.go L57-L89 (target internal/retrieve/retrieve.go) truncates identically. `if limit > 0 && len(results) > limit` |
| FOUND | internal/retrieve/retrieve.go:610-616, 917-921:None | Relevance-mode re-rank sorts solely by descending fused RRF score (coverage and routine partition removed). | grepai search/hybrid.go L57-L89 (target internal/retrieve/retrieve.go) sorts fused results by descending score; the removed coverage/routine heuristic was local, deletable under doctrine rule 4. `return results[i].Score > results[j].Score` |
| MADE | internal/retrieve/retrieve.go:553-570:None | Branch `if p.Sort == "newest" \|\| p.Sort == "oldest"` bypasses RRF fusion entirely and instead concatenates exact+stemmed hit lists, dedups them, and truncates t | No cited source contains this branch. meilisearch hybrid.rs L51-L57 is a comparator match arm on ScoreValue::Sort; it contains no branch that skips fusion. clickclack search_pages.go L56-L75 builds sort-dependent cursor   |
| MADE | internal/retrieve/retrieve.go:851-871:None | Same newest/oldest fusion-bypass branch with dedup-preserving-SQL-order merge in MatchAnchors. | Same as above: neither the meilisearch nor the clickclack fetched lines contain a branch that replaces RRF with an ordered dedup merge for time-sort modes.  |
| MADE | internal/retrieve/retrieve.go:592-595, 893-899:None | Nil-map guard `if hitScores != nil` / `if anchorScores != nil` before reading the fused score, defaulting fused to 0. | Added conditional fallback; grepai always reads from a populated score map and has no such guard. A defensive condition the source lacks is LOGIC under doctrine ('What wiring means, strictly').  |
| MADE | internal/retrieve/retrieve.go:893-896:None | Key derivation fallback in MatchAnchors: use a.UUID, else SessionID + ":" + ISO. | grepai keys results by a single chunk ID (`id := result.Chunk.ID`); the empty-UUID branch and the string concatenation of two fields have no cited upstream line (RubyHeron 359 case B).  |

### run 8 @ 01:47:12 local — verdict REJECT, 12 findings

| class | file:line | decision | reason / upstream_quote |
|---|---|---|---|
| POLICY | internal/index/consolidated.go:1464:None | SettleWindow = 60s freshness settle window | Labeled `// policy: 2026-09-04 60s settle see docs/design/decision-references.md#D10`; operational constant with date and design-doc reference.  |
| POLICY | internal/store/store.go:345:None | ROMmapSize = 0x7fff0000 read-only mmap window | Labeled `// policy: 2026-09-04 mmap 0x7fff0000 = SQLite ceiling see docs/design/decision-references.md#D2`; constant only, no control flow added.  |
| FOUND | internal/store/fts.go:224-245:None | Wrap the FTS5 query in a subquery and compute a window rank over the bm25 score (`RANK() OVER (ORDER BY bm25_score)`), with the inner query keeping LIMIT/OFFSET | paradedb reciprocal_rank_fusion.sql L32-L60 (trailer targets internal/store/fts.go) computes a window RANK over the relevance score of a limited inner query; the DESC->ASC flip is bm25() polarity adaptation (sqlite.org/f `RANK() OVER (ORDER BY score DESC) AS rank` |
| FOUND | internal/store/fts.go:238-243, 344-350:None | Inner query keeps its ORDER BY and LIMIT before ranking is applied. | paradedb L32-L60 (target internal/store/fts.go) orders and limits the inner select before the outer RANK. `ORDER BY pdb.score(order_id) DESC` |
| FOUND | internal/retrieve/retrieve.go:412, 462:None | RRF contribution computed as 1/(k + SQL rank) instead of 1/(k + positional index + 1). | paradedb reciprocal_rank_fusion.sql L49-L68 (trailer targets internal/retrieve/retrieve.go) fuses using the SQL window rank directly with no +1. `1.0 / (60 + rank) AS s FROM order_search` |
| FOUND | internal/retrieve/retrieve.go:444, 485:None | Tie-break after equal fused score is ascending row ID (replacing ISO-desc / SessionID-asc tie-breaks). | paradedb L49-L68 (target internal/retrieve/retrieve.go) orders by score descending then by the row identifier ascending; the removed ISO/SessionID tie-breaks were local inventions, deleted under doctrine rule 4. `ORDER BY score DESC, o.order_id` |
| FOUND | internal/retrieve/retrieve.go:449, 490:None | Truncate fused results to the requested limit when limit > 0. | grepai search/hybrid.go L57-L89 (target internal/retrieve/retrieve.go) truncates identically. `if limit > 0 && len(results) > limit` |
| FOUND | internal/retrieve/retrieve.go:610-616, 917-921:None | Relevance-mode re-rank sorts solely by descending fused RRF score (coverage and routine partition removed). | grepai search/hybrid.go L57-L89 (target internal/retrieve/retrieve.go) sorts fused results by descending score; the removed coverage/routine heuristic was local, deletable under doctrine rule 4. `return results[i].Score > results[j].Score` |
| MADE | internal/retrieve/retrieve.go:553-570:None | Branch `if p.Sort == "newest" \|\| p.Sort == "oldest"` bypasses RRF fusion entirely and instead concatenates exact+stemmed hit lists, dedups them, and truncates t | No cited source contains this branch. meilisearch hybrid.rs L51-L57 is a comparator match arm on ScoreValue::Sort; it contains no branch that skips fusion. clickclack search_pages.go L56-L75 builds sort-dependent cursor   |
| MADE | internal/retrieve/retrieve.go:851-871:None | Same newest/oldest fusion-bypass branch with dedup-preserving-SQL-order merge in MatchAnchors. | Same as above: neither the meilisearch nor the clickclack fetched lines contain a branch that replaces RRF with an ordered dedup merge for time-sort modes.  |
| MADE | internal/retrieve/retrieve.go:592-595, 893-899:None | Nil-map guard `if hitScores != nil` / `if anchorScores != nil` before reading the fused score, defaulting fused to 0. | Added conditional fallback; grepai always reads from a populated score map and has no such guard. A defensive condition the source lacks is LOGIC under doctrine ('What wiring means, strictly').  |
| MADE | internal/retrieve/retrieve.go:893-896:None | Key derivation fallback in MatchAnchors: use a.UUID, else SessionID + ":" + ISO. | grepai keys results by a single chunk ID (`id := result.Chunk.ID`); the empty-UUID branch and the string concatenation of two fields have no cited upstream line (RubyHeron 359 case B).  |

### run 9 @ 01:47:16 local — verdict REJECT, 12 findings

| class | file:line | decision | reason / upstream_quote |
|---|---|---|---|
| POLICY | internal/index/consolidated.go:1464:None | SettleWindow = 60s freshness settle window | Labeled `// policy: 2026-09-04 60s settle see docs/design/decision-references.md#D10`; operational constant with date and design-doc reference.  |
| POLICY | internal/store/store.go:345:None | ROMmapSize = 0x7fff0000 read-only mmap window | Labeled `// policy: 2026-09-04 mmap 0x7fff0000 = SQLite ceiling see docs/design/decision-references.md#D2`; constant only, no control flow added.  |
| FOUND | internal/store/fts.go:224-245:None | Wrap the FTS5 query in a subquery and compute a window rank over the bm25 score (`RANK() OVER (ORDER BY bm25_score)`), with the inner query keeping LIMIT/OFFSET | paradedb reciprocal_rank_fusion.sql L32-L60 (trailer targets internal/store/fts.go) computes a window RANK over the relevance score of a limited inner query; the DESC->ASC flip is bm25() polarity adaptation (sqlite.org/f `RANK() OVER (ORDER BY score DESC) AS rank` |
| FOUND | internal/store/fts.go:238-243, 344-350:None | Inner query keeps its ORDER BY and LIMIT before ranking is applied. | paradedb L32-L60 (target internal/store/fts.go) orders and limits the inner select before the outer RANK. `ORDER BY pdb.score(order_id) DESC` |
| FOUND | internal/retrieve/retrieve.go:412, 462:None | RRF contribution computed as 1/(k + SQL rank) instead of 1/(k + positional index + 1). | paradedb reciprocal_rank_fusion.sql L49-L68 (trailer targets internal/retrieve/retrieve.go) fuses using the SQL window rank directly with no +1. `1.0 / (60 + rank) AS s FROM order_search` |
| FOUND | internal/retrieve/retrieve.go:444, 485:None | Tie-break after equal fused score is ascending row ID (replacing ISO-desc / SessionID-asc tie-breaks). | paradedb L49-L68 (target internal/retrieve/retrieve.go) orders by score descending then by the row identifier ascending; the removed ISO/SessionID tie-breaks were local inventions, deleted under doctrine rule 4. `ORDER BY score DESC, o.order_id` |
| FOUND | internal/retrieve/retrieve.go:449, 490:None | Truncate fused results to the requested limit when limit > 0. | grepai search/hybrid.go L57-L89 (target internal/retrieve/retrieve.go) truncates identically. `if limit > 0 && len(results) > limit` |
| FOUND | internal/retrieve/retrieve.go:610-616, 917-921:None | Relevance-mode re-rank sorts solely by descending fused RRF score (coverage and routine partition removed). | grepai search/hybrid.go L57-L89 (target internal/retrieve/retrieve.go) sorts fused results by descending score; the removed coverage/routine heuristic was local, deletable under doctrine rule 4. `return results[i].Score > results[j].Score` |
| MADE | internal/retrieve/retrieve.go:553-570:None | Branch `if p.Sort == "newest" \|\| p.Sort == "oldest"` bypasses RRF fusion entirely and instead concatenates exact+stemmed hit lists, dedups them, and truncates t | No cited source contains this branch. meilisearch hybrid.rs L51-L57 is a comparator match arm on ScoreValue::Sort; it contains no branch that skips fusion. clickclack search_pages.go L56-L75 builds sort-dependent cursor   |
| MADE | internal/retrieve/retrieve.go:851-871:None | Same newest/oldest fusion-bypass branch with dedup-preserving-SQL-order merge in MatchAnchors. | Same as above: neither the meilisearch nor the clickclack fetched lines contain a branch that replaces RRF with an ordered dedup merge for time-sort modes.  |
| MADE | internal/retrieve/retrieve.go:592-595, 893-899:None | Nil-map guard `if hitScores != nil` / `if anchorScores != nil` before reading the fused score, defaulting fused to 0. | Added conditional fallback; grepai always reads from a populated score map and has no such guard. A defensive condition the source lacks is LOGIC under doctrine ('What wiring means, strictly').  |
| MADE | internal/retrieve/retrieve.go:893-896:None | Key derivation fallback in MatchAnchors: use a.UUID, else SessionID + ":" + ISO. | grepai keys results by a single chunk ID (`id := result.Chunk.ID`); the empty-UUID branch and the string concatenation of two fields have no cited upstream line (RubyHeron 359 case B).  |

### run 10 @ 01:47:20 local — verdict REJECT, 12 findings

| class | file:line | decision | reason / upstream_quote |
|---|---|---|---|
| POLICY | internal/index/consolidated.go:1464:None | SettleWindow = 60s freshness settle window | Labeled `// policy: 2026-09-04 60s settle see docs/design/decision-references.md#D10`; operational constant with date and design-doc reference.  |
| POLICY | internal/store/store.go:345:None | ROMmapSize = 0x7fff0000 read-only mmap window | Labeled `// policy: 2026-09-04 mmap 0x7fff0000 = SQLite ceiling see docs/design/decision-references.md#D2`; constant only, no control flow added.  |
| FOUND | internal/store/fts.go:224-245:None | Wrap the FTS5 query in a subquery and compute a window rank over the bm25 score (`RANK() OVER (ORDER BY bm25_score)`), with the inner query keeping LIMIT/OFFSET | paradedb reciprocal_rank_fusion.sql L32-L60 (trailer targets internal/store/fts.go) computes a window RANK over the relevance score of a limited inner query; the DESC->ASC flip is bm25() polarity adaptation (sqlite.org/f `RANK() OVER (ORDER BY score DESC) AS rank` |
| FOUND | internal/store/fts.go:238-243, 344-350:None | Inner query keeps its ORDER BY and LIMIT before ranking is applied. | paradedb L32-L60 (target internal/store/fts.go) orders and limits the inner select before the outer RANK. `ORDER BY pdb.score(order_id) DESC` |
| FOUND | internal/retrieve/retrieve.go:412, 462:None | RRF contribution computed as 1/(k + SQL rank) instead of 1/(k + positional index + 1). | paradedb reciprocal_rank_fusion.sql L49-L68 (trailer targets internal/retrieve/retrieve.go) fuses using the SQL window rank directly with no +1. `1.0 / (60 + rank) AS s FROM order_search` |
| FOUND | internal/retrieve/retrieve.go:444, 485:None | Tie-break after equal fused score is ascending row ID (replacing ISO-desc / SessionID-asc tie-breaks). | paradedb L49-L68 (target internal/retrieve/retrieve.go) orders by score descending then by the row identifier ascending; the removed ISO/SessionID tie-breaks were local inventions, deleted under doctrine rule 4. `ORDER BY score DESC, o.order_id` |
| FOUND | internal/retrieve/retrieve.go:449, 490:None | Truncate fused results to the requested limit when limit > 0. | grepai search/hybrid.go L57-L89 (target internal/retrieve/retrieve.go) truncates identically. `if limit > 0 && len(results) > limit` |
| FOUND | internal/retrieve/retrieve.go:610-616, 917-921:None | Relevance-mode re-rank sorts solely by descending fused RRF score (coverage and routine partition removed). | grepai search/hybrid.go L57-L89 (target internal/retrieve/retrieve.go) sorts fused results by descending score; the removed coverage/routine heuristic was local, deletable under doctrine rule 4. `return results[i].Score > results[j].Score` |
| MADE | internal/retrieve/retrieve.go:553-570:None | Branch `if p.Sort == "newest" \|\| p.Sort == "oldest"` bypasses RRF fusion entirely and instead concatenates exact+stemmed hit lists, dedups them, and truncates t | No cited source contains this branch. meilisearch hybrid.rs L51-L57 is a comparator match arm on ScoreValue::Sort; it contains no branch that skips fusion. clickclack search_pages.go L56-L75 builds sort-dependent cursor   |
| MADE | internal/retrieve/retrieve.go:851-871:None | Same newest/oldest fusion-bypass branch with dedup-preserving-SQL-order merge in MatchAnchors. | Same as above: neither the meilisearch nor the clickclack fetched lines contain a branch that replaces RRF with an ordered dedup merge for time-sort modes.  |
| MADE | internal/retrieve/retrieve.go:592-595, 893-899:None | Nil-map guard `if hitScores != nil` / `if anchorScores != nil` before reading the fused score, defaulting fused to 0. | Added conditional fallback; grepai always reads from a populated score map and has no such guard. A defensive condition the source lacks is LOGIC under doctrine ('What wiring means, strictly').  |
| MADE | internal/retrieve/retrieve.go:893-896:None | Key derivation fallback in MatchAnchors: use a.UUID, else SessionID + ":" + ISO. | grepai keys results by a single chunk ID (`id := result.Chunk.ID`); the empty-UUID branch and the string concatenation of two fields have no cited upstream line (RubyHeron 359 case B).  |

## claude-sub/claude-fable-5-1  (10 runs, raw files: /tmp/judge-claude-fable-5-1/resp/)

### run 1 @ 05:54:42 local — verdict REJECT, 19 findings

| class | file:line | decision | reason / upstream_quote |
|---|---|---|---|
| POLICY | None:None | internal/index/consolidated.go:1464 SettleWindow = 60s labeled as measured operational constant | // policy: 2026-09-04 60s settle see docs/design/decision-references.md#D10 label on an operational timeout  |
| POLICY | None:None | internal/store/store.go:345 ROMmapSize = 0x7fff0000 labeled | // policy: 2026-09-04 mmap 0x7fff0000 = SQLite ceiling see docs/design/decision-references.md#D2  |
| FOUND | None:None | internal/query/query.go:33-49 deletion of local regex sanitizer heuristics (reFTS5Structural, reLeadStar, reRunStar, reLeadBool, reTrailBool, reDottedID, reProt | Deletion of our own uncited heuristics; doctrine rule 4 losers get deleted. Retained sanitizer is the cited zk-org/zk ConvertQuery. `func ConvertQuery(query string) string {` |
| FOUND | None:None | internal/query/query.go:149-188 deletion of EscapeFTS5Query (ccrider search.go port) in favour of zk ConvertQuery as the single FTS5 escaper | The deleted function's upstream (ccrider search.go) is not a cited trailer in this range; the surviving escaper traces to zk-org/zk fts5.go L6-L104 which quotes each term `out.WriteString(`"` + term + `"`)` |
| FOUND | None:None | internal/store/fts.go:234-246 and :340-351 window RANK() OVER (ORDER BY bm25_score) computed over an inner subquery that applies WHERE/ORDER/LIMIT first | paradedb reciprocal_rank_fusion.sql L32-L60 -> fts.go: RANK() window over an inner LIMITed subquery scoring rows; ORDER BY score DESC adapted to SQLite bm25() negative polarity (ascending) is language scaffolding `RANK() OVER (ORDER BY score DESC) AS rank` |
| FOUND | None:None | internal/store/fts.go:239-240 and :347-348 inner subquery selects bm25(tbl) AS bm25_score as the rank expression | openclaw/clickclack search_pages.go L56-L75 -> fts.go uses bm25(messages_fts) as the rank expression; paradedb selects pdb.score(order_id) AS score inside the subquery `rankExpression := "bm25(messages_fts)"` |
| FOUND | None:None | internal/store/fts.go:241-243 and :349-351 inner subquery ordered by orderClause(s) with LIMIT (and OFFSET) applied before ranking | paradedb L32-L60 -> fts.go: inner SELECT ... ORDER BY pdb.score(order_id) DESC LIMIT 20 before the RANK window `SELECT order_id, pdb.score(order_id) AS score` |
| FOUND | None:None | internal/store/fts.go:243 and :351 outer query re-applies orderClause(s) after the window | paradedb L49-L68 final SELECT orders the ranked output; wacli L99-L105 -> fts.go orders by bm25 then rowid `ORDER BY score DESC, o.order_id` |
| FOUND | None:None | internal/store/fts.go SearchHit/SearchAnchor gain ID, BM25, Rank fields and scan them | Struct field assignment and scan plumbing of the cited paradedb rank/score columns; wiring per doctrine `RANK() OVER (ORDER BY score DESC) AS rank` |
| FOUND | None:None | internal/retrieve/retrieve.go:411 and :460 RRF score term 1/(k + Rank) using the SQL RANK() value instead of list position, k=60 | paradedb L49-L68 -> retrieve.go computes reciprocal of (60 + rank) from the window rank `1.0 / (60 + rank) AS s` |
| FOUND | None:None | internal/retrieve/retrieve.go:412 and :461 summing RRF contributions across lists into a per-key score map | grepai hybrid.go L57-L89 -> retrieve.go accumulates scores[id] across lists; paradedb sum(rrf.s) AS score `scores[id] += 1.0 / (k + float32(rank) + 1)` |
| FOUND | None:None | internal/retrieve/retrieve.go:415-427 and :464-476 results assembled by first-seen list order with a seen map instead of map iteration | Ordered slice beside a map is Go scaffolding per the gate procedure; the dedup-by-id itself is grepai's chunkMap dedup `Results are deduplicated by chunk ID and sorted by combined RRF score.` |
| FOUND | None:None | internal/retrieve/retrieve.go:441-445 and :482-487 sort by fused score DESC then message ID ASC; ISO DESC tie-break removed | paradedb L49-L68 -> retrieve.go final ORDER BY score DESC, o.order_id; the removed ISO tie-break was our own uncited invention (rule 4) `ORDER BY score DESC, o.order_id` |
| FOUND | None:None | internal/retrieve/retrieve.go:447 and :488 truncate fused results to limit when limit > 0 | grepai hybrid.go L57-L89 -> retrieve.go `if limit > 0 && len(results) > limit {` |
| FOUND | None:None | internal/retrieve/retrieve.go:404 and :455 rrfAnchors/rrfHits return the scores map as a second value | Function-return pass-through of the grepai score map; wiring `scores := make(map[string]float32)` |
| MADE | None:None | internal/retrieve/retrieve.go:555-571 and :853-872 new mode branch: when p.Sort is "newest" or "oldest", skip RRF and instead concatenate exact list then stemme | No cited source contains this branch. meilisearch hybrid.rs L51-L57 is a fragment comparing ScoreValue::Sort pairs and says nothing about bypassing fusion under an explicit sort. clickclack L56-L75 selects cursor predica  |
| MADE | None:None | internal/retrieve/retrieve.go:592-596 and :893-900 fused defaults to 0 when no fusion ran (hitScores/anchorScores nil), so the relevance re-rank becomes a stabl | A fallback the sources lack: grepai sorts only lists that were fused and every entry has a score; no cited line assigns a zero score to unfused hits or sorts an unfused list by a zero-valued fused key while relying on st  |
| FOUND | None:None | internal/retrieve/retrieve.go:614-618 and :917-921 replacement of the coverage/routine re-rank with sort by fused score DESC (relevance mode) | Deleted coverage/routine comparator was our own uncited heuristic (rule 4). New comparator is grepai hybrid.go L57-L89 -> retrieve.go `return results[i].Score > results[j].Score` |
| FOUND | None:None | internal/store/store.go:135-137 ExactSQL comment now describes plain unicode61 without porter stemming | Comment-only change; matches ccrider schema.go L86-L122 code index tokenizer `tokenize='unicode61'` |

### run 2 @ 05:54:43 local — verdict REJECT, 19 findings

| class | file:line | decision | reason / upstream_quote |
|---|---|---|---|
| POLICY | None:None | internal/index/consolidated.go:1464 SettleWindow = 60s labeled as measured operational constant | // policy: 2026-09-04 60s settle see docs/design/decision-references.md#D10 label on an operational timeout  |
| POLICY | None:None | internal/store/store.go:345 ROMmapSize = 0x7fff0000 labeled | // policy: 2026-09-04 mmap 0x7fff0000 = SQLite ceiling see docs/design/decision-references.md#D2  |
| FOUND | None:None | internal/query/query.go:33-49 deletion of local regex sanitizer heuristics (reFTS5Structural, reLeadStar, reRunStar, reLeadBool, reTrailBool, reDottedID, reProt | Deletion of our own uncited heuristics; doctrine rule 4 losers get deleted. Retained sanitizer is the cited zk-org/zk ConvertQuery. `func ConvertQuery(query string) string {` |
| FOUND | None:None | internal/query/query.go:149-188 deletion of EscapeFTS5Query (ccrider search.go port) in favour of zk ConvertQuery as the single FTS5 escaper | The deleted function's upstream (ccrider search.go) is not a cited trailer in this range; the surviving escaper traces to zk-org/zk fts5.go L6-L104 which quotes each term `out.WriteString(`"` + term + `"`)` |
| FOUND | None:None | internal/store/fts.go:234-246 and :340-351 window RANK() OVER (ORDER BY bm25_score) computed over an inner subquery that applies WHERE/ORDER/LIMIT first | paradedb reciprocal_rank_fusion.sql L32-L60 -> fts.go: RANK() window over an inner LIMITed subquery scoring rows; ORDER BY score DESC adapted to SQLite bm25() negative polarity (ascending) is language scaffolding `RANK() OVER (ORDER BY score DESC) AS rank` |
| FOUND | None:None | internal/store/fts.go:239-240 and :347-348 inner subquery selects bm25(tbl) AS bm25_score as the rank expression | openclaw/clickclack search_pages.go L56-L75 -> fts.go uses bm25(messages_fts) as the rank expression; paradedb selects pdb.score(order_id) AS score inside the subquery `rankExpression := "bm25(messages_fts)"` |
| FOUND | None:None | internal/store/fts.go:241-243 and :349-351 inner subquery ordered by orderClause(s) with LIMIT (and OFFSET) applied before ranking | paradedb L32-L60 -> fts.go: inner SELECT ... ORDER BY pdb.score(order_id) DESC LIMIT 20 before the RANK window `SELECT order_id, pdb.score(order_id) AS score` |
| FOUND | None:None | internal/store/fts.go:243 and :351 outer query re-applies orderClause(s) after the window | paradedb L49-L68 final SELECT orders the ranked output; wacli L99-L105 -> fts.go orders by bm25 then rowid `ORDER BY score DESC, o.order_id` |
| FOUND | None:None | internal/store/fts.go SearchHit/SearchAnchor gain ID, BM25, Rank fields and scan them | Struct field assignment and scan plumbing of the cited paradedb rank/score columns; wiring per doctrine `RANK() OVER (ORDER BY score DESC) AS rank` |
| FOUND | None:None | internal/retrieve/retrieve.go:411 and :460 RRF score term 1/(k + Rank) using the SQL RANK() value instead of list position, k=60 | paradedb L49-L68 -> retrieve.go computes reciprocal of (60 + rank) from the window rank `1.0 / (60 + rank) AS s` |
| FOUND | None:None | internal/retrieve/retrieve.go:412 and :461 summing RRF contributions across lists into a per-key score map | grepai hybrid.go L57-L89 -> retrieve.go accumulates scores[id] across lists; paradedb sum(rrf.s) AS score `scores[id] += 1.0 / (k + float32(rank) + 1)` |
| FOUND | None:None | internal/retrieve/retrieve.go:415-427 and :464-476 results assembled by first-seen list order with a seen map instead of map iteration | Ordered slice beside a map is Go scaffolding per the gate procedure; the dedup-by-id itself is grepai's chunkMap dedup `Results are deduplicated by chunk ID and sorted by combined RRF score.` |
| FOUND | None:None | internal/retrieve/retrieve.go:441-445 and :482-487 sort by fused score DESC then message ID ASC; ISO DESC tie-break removed | paradedb L49-L68 -> retrieve.go final ORDER BY score DESC, o.order_id; the removed ISO tie-break was our own uncited invention (rule 4) `ORDER BY score DESC, o.order_id` |
| FOUND | None:None | internal/retrieve/retrieve.go:447 and :488 truncate fused results to limit when limit > 0 | grepai hybrid.go L57-L89 -> retrieve.go `if limit > 0 && len(results) > limit {` |
| FOUND | None:None | internal/retrieve/retrieve.go:404 and :455 rrfAnchors/rrfHits return the scores map as a second value | Function-return pass-through of the grepai score map; wiring `scores := make(map[string]float32)` |
| MADE | None:None | internal/retrieve/retrieve.go:555-571 and :853-872 new mode branch: when p.Sort is "newest" or "oldest", skip RRF and instead concatenate exact list then stemme | No cited source contains this branch. meilisearch hybrid.rs L51-L57 is a fragment comparing ScoreValue::Sort pairs and says nothing about bypassing fusion under an explicit sort. clickclack L56-L75 selects cursor predica  |
| MADE | None:None | internal/retrieve/retrieve.go:592-596 and :893-900 fused defaults to 0 when no fusion ran (hitScores/anchorScores nil), so the relevance re-rank becomes a stabl | A fallback the sources lack: grepai sorts only lists that were fused and every entry has a score; no cited line assigns a zero score to unfused hits or sorts an unfused list by a zero-valued fused key while relying on st  |
| FOUND | None:None | internal/retrieve/retrieve.go:614-618 and :917-921 replacement of the coverage/routine re-rank with sort by fused score DESC (relevance mode) | Deleted coverage/routine comparator was our own uncited heuristic (rule 4). New comparator is grepai hybrid.go L57-L89 -> retrieve.go `return results[i].Score > results[j].Score` |
| FOUND | None:None | internal/store/store.go:135-137 ExactSQL comment now describes plain unicode61 without porter stemming | Comment-only change; matches ccrider schema.go L86-L122 code index tokenizer `tokenize='unicode61'` |

### run 3 @ 05:54:45 local — verdict REJECT, 19 findings

| class | file:line | decision | reason / upstream_quote |
|---|---|---|---|
| POLICY | None:None | internal/index/consolidated.go:1464 SettleWindow = 60s labeled as measured operational constant | // policy: 2026-09-04 60s settle see docs/design/decision-references.md#D10 label on an operational timeout  |
| POLICY | None:None | internal/store/store.go:345 ROMmapSize = 0x7fff0000 labeled | // policy: 2026-09-04 mmap 0x7fff0000 = SQLite ceiling see docs/design/decision-references.md#D2  |
| FOUND | None:None | internal/query/query.go:33-49 deletion of local regex sanitizer heuristics (reFTS5Structural, reLeadStar, reRunStar, reLeadBool, reTrailBool, reDottedID, reProt | Deletion of our own uncited heuristics; doctrine rule 4 losers get deleted. Retained sanitizer is the cited zk-org/zk ConvertQuery. `func ConvertQuery(query string) string {` |
| FOUND | None:None | internal/query/query.go:149-188 deletion of EscapeFTS5Query (ccrider search.go port) in favour of zk ConvertQuery as the single FTS5 escaper | The deleted function's upstream (ccrider search.go) is not a cited trailer in this range; the surviving escaper traces to zk-org/zk fts5.go L6-L104 which quotes each term `out.WriteString(`"` + term + `"`)` |
| FOUND | None:None | internal/store/fts.go:234-246 and :340-351 window RANK() OVER (ORDER BY bm25_score) computed over an inner subquery that applies WHERE/ORDER/LIMIT first | paradedb reciprocal_rank_fusion.sql L32-L60 -> fts.go: RANK() window over an inner LIMITed subquery scoring rows; ORDER BY score DESC adapted to SQLite bm25() negative polarity (ascending) is language scaffolding `RANK() OVER (ORDER BY score DESC) AS rank` |
| FOUND | None:None | internal/store/fts.go:239-240 and :347-348 inner subquery selects bm25(tbl) AS bm25_score as the rank expression | openclaw/clickclack search_pages.go L56-L75 -> fts.go uses bm25(messages_fts) as the rank expression; paradedb selects pdb.score(order_id) AS score inside the subquery `rankExpression := "bm25(messages_fts)"` |
| FOUND | None:None | internal/store/fts.go:241-243 and :349-351 inner subquery ordered by orderClause(s) with LIMIT (and OFFSET) applied before ranking | paradedb L32-L60 -> fts.go: inner SELECT ... ORDER BY pdb.score(order_id) DESC LIMIT 20 before the RANK window `SELECT order_id, pdb.score(order_id) AS score` |
| FOUND | None:None | internal/store/fts.go:243 and :351 outer query re-applies orderClause(s) after the window | paradedb L49-L68 final SELECT orders the ranked output; wacli L99-L105 -> fts.go orders by bm25 then rowid `ORDER BY score DESC, o.order_id` |
| FOUND | None:None | internal/store/fts.go SearchHit/SearchAnchor gain ID, BM25, Rank fields and scan them | Struct field assignment and scan plumbing of the cited paradedb rank/score columns; wiring per doctrine `RANK() OVER (ORDER BY score DESC) AS rank` |
| FOUND | None:None | internal/retrieve/retrieve.go:411 and :460 RRF score term 1/(k + Rank) using the SQL RANK() value instead of list position, k=60 | paradedb L49-L68 -> retrieve.go computes reciprocal of (60 + rank) from the window rank `1.0 / (60 + rank) AS s` |
| FOUND | None:None | internal/retrieve/retrieve.go:412 and :461 summing RRF contributions across lists into a per-key score map | grepai hybrid.go L57-L89 -> retrieve.go accumulates scores[id] across lists; paradedb sum(rrf.s) AS score `scores[id] += 1.0 / (k + float32(rank) + 1)` |
| FOUND | None:None | internal/retrieve/retrieve.go:415-427 and :464-476 results assembled by first-seen list order with a seen map instead of map iteration | Ordered slice beside a map is Go scaffolding per the gate procedure; the dedup-by-id itself is grepai's chunkMap dedup `Results are deduplicated by chunk ID and sorted by combined RRF score.` |
| FOUND | None:None | internal/retrieve/retrieve.go:441-445 and :482-487 sort by fused score DESC then message ID ASC; ISO DESC tie-break removed | paradedb L49-L68 -> retrieve.go final ORDER BY score DESC, o.order_id; the removed ISO tie-break was our own uncited invention (rule 4) `ORDER BY score DESC, o.order_id` |
| FOUND | None:None | internal/retrieve/retrieve.go:447 and :488 truncate fused results to limit when limit > 0 | grepai hybrid.go L57-L89 -> retrieve.go `if limit > 0 && len(results) > limit {` |
| FOUND | None:None | internal/retrieve/retrieve.go:404 and :455 rrfAnchors/rrfHits return the scores map as a second value | Function-return pass-through of the grepai score map; wiring `scores := make(map[string]float32)` |
| MADE | None:None | internal/retrieve/retrieve.go:555-571 and :853-872 new mode branch: when p.Sort is "newest" or "oldest", skip RRF and instead concatenate exact list then stemme | No cited source contains this branch. meilisearch hybrid.rs L51-L57 is a fragment comparing ScoreValue::Sort pairs and says nothing about bypassing fusion under an explicit sort. clickclack L56-L75 selects cursor predica  |
| MADE | None:None | internal/retrieve/retrieve.go:592-596 and :893-900 fused defaults to 0 when no fusion ran (hitScores/anchorScores nil), so the relevance re-rank becomes a stabl | A fallback the sources lack: grepai sorts only lists that were fused and every entry has a score; no cited line assigns a zero score to unfused hits or sorts an unfused list by a zero-valued fused key while relying on st  |
| FOUND | None:None | internal/retrieve/retrieve.go:614-618 and :917-921 replacement of the coverage/routine re-rank with sort by fused score DESC (relevance mode) | Deleted coverage/routine comparator was our own uncited heuristic (rule 4). New comparator is grepai hybrid.go L57-L89 -> retrieve.go `return results[i].Score > results[j].Score` |
| FOUND | None:None | internal/store/store.go:135-137 ExactSQL comment now describes plain unicode61 without porter stemming | Comment-only change; matches ccrider schema.go L86-L122 code index tokenizer `tokenize='unicode61'` |

### run 4 @ 05:54:46 local — verdict REJECT, 19 findings

| class | file:line | decision | reason / upstream_quote |
|---|---|---|---|
| POLICY | None:None | internal/index/consolidated.go:1464 SettleWindow = 60s labeled as measured operational constant | // policy: 2026-09-04 60s settle see docs/design/decision-references.md#D10 label on an operational timeout  |
| POLICY | None:None | internal/store/store.go:345 ROMmapSize = 0x7fff0000 labeled | // policy: 2026-09-04 mmap 0x7fff0000 = SQLite ceiling see docs/design/decision-references.md#D2  |
| FOUND | None:None | internal/query/query.go:33-49 deletion of local regex sanitizer heuristics (reFTS5Structural, reLeadStar, reRunStar, reLeadBool, reTrailBool, reDottedID, reProt | Deletion of our own uncited heuristics; doctrine rule 4 losers get deleted. Retained sanitizer is the cited zk-org/zk ConvertQuery. `func ConvertQuery(query string) string {` |
| FOUND | None:None | internal/query/query.go:149-188 deletion of EscapeFTS5Query (ccrider search.go port) in favour of zk ConvertQuery as the single FTS5 escaper | The deleted function's upstream (ccrider search.go) is not a cited trailer in this range; the surviving escaper traces to zk-org/zk fts5.go L6-L104 which quotes each term `out.WriteString(`"` + term + `"`)` |
| FOUND | None:None | internal/store/fts.go:234-246 and :340-351 window RANK() OVER (ORDER BY bm25_score) computed over an inner subquery that applies WHERE/ORDER/LIMIT first | paradedb reciprocal_rank_fusion.sql L32-L60 -> fts.go: RANK() window over an inner LIMITed subquery scoring rows; ORDER BY score DESC adapted to SQLite bm25() negative polarity (ascending) is language scaffolding `RANK() OVER (ORDER BY score DESC) AS rank` |
| FOUND | None:None | internal/store/fts.go:239-240 and :347-348 inner subquery selects bm25(tbl) AS bm25_score as the rank expression | openclaw/clickclack search_pages.go L56-L75 -> fts.go uses bm25(messages_fts) as the rank expression; paradedb selects pdb.score(order_id) AS score inside the subquery `rankExpression := "bm25(messages_fts)"` |
| FOUND | None:None | internal/store/fts.go:241-243 and :349-351 inner subquery ordered by orderClause(s) with LIMIT (and OFFSET) applied before ranking | paradedb L32-L60 -> fts.go: inner SELECT ... ORDER BY pdb.score(order_id) DESC LIMIT 20 before the RANK window `SELECT order_id, pdb.score(order_id) AS score` |
| FOUND | None:None | internal/store/fts.go:243 and :351 outer query re-applies orderClause(s) after the window | paradedb L49-L68 final SELECT orders the ranked output; wacli L99-L105 -> fts.go orders by bm25 then rowid `ORDER BY score DESC, o.order_id` |
| FOUND | None:None | internal/store/fts.go SearchHit/SearchAnchor gain ID, BM25, Rank fields and scan them | Struct field assignment and scan plumbing of the cited paradedb rank/score columns; wiring per doctrine `RANK() OVER (ORDER BY score DESC) AS rank` |
| FOUND | None:None | internal/retrieve/retrieve.go:411 and :460 RRF score term 1/(k + Rank) using the SQL RANK() value instead of list position, k=60 | paradedb L49-L68 -> retrieve.go computes reciprocal of (60 + rank) from the window rank `1.0 / (60 + rank) AS s` |
| FOUND | None:None | internal/retrieve/retrieve.go:412 and :461 summing RRF contributions across lists into a per-key score map | grepai hybrid.go L57-L89 -> retrieve.go accumulates scores[id] across lists; paradedb sum(rrf.s) AS score `scores[id] += 1.0 / (k + float32(rank) + 1)` |
| FOUND | None:None | internal/retrieve/retrieve.go:415-427 and :464-476 results assembled by first-seen list order with a seen map instead of map iteration | Ordered slice beside a map is Go scaffolding per the gate procedure; the dedup-by-id itself is grepai's chunkMap dedup `Results are deduplicated by chunk ID and sorted by combined RRF score.` |
| FOUND | None:None | internal/retrieve/retrieve.go:441-445 and :482-487 sort by fused score DESC then message ID ASC; ISO DESC tie-break removed | paradedb L49-L68 -> retrieve.go final ORDER BY score DESC, o.order_id; the removed ISO tie-break was our own uncited invention (rule 4) `ORDER BY score DESC, o.order_id` |
| FOUND | None:None | internal/retrieve/retrieve.go:447 and :488 truncate fused results to limit when limit > 0 | grepai hybrid.go L57-L89 -> retrieve.go `if limit > 0 && len(results) > limit {` |
| FOUND | None:None | internal/retrieve/retrieve.go:404 and :455 rrfAnchors/rrfHits return the scores map as a second value | Function-return pass-through of the grepai score map; wiring `scores := make(map[string]float32)` |
| MADE | None:None | internal/retrieve/retrieve.go:555-571 and :853-872 new mode branch: when p.Sort is "newest" or "oldest", skip RRF and instead concatenate exact list then stemme | No cited source contains this branch. meilisearch hybrid.rs L51-L57 is a fragment comparing ScoreValue::Sort pairs and says nothing about bypassing fusion under an explicit sort. clickclack L56-L75 selects cursor predica  |
| MADE | None:None | internal/retrieve/retrieve.go:592-596 and :893-900 fused defaults to 0 when no fusion ran (hitScores/anchorScores nil), so the relevance re-rank becomes a stabl | A fallback the sources lack: grepai sorts only lists that were fused and every entry has a score; no cited line assigns a zero score to unfused hits or sorts an unfused list by a zero-valued fused key while relying on st  |
| FOUND | None:None | internal/retrieve/retrieve.go:614-618 and :917-921 replacement of the coverage/routine re-rank with sort by fused score DESC (relevance mode) | Deleted coverage/routine comparator was our own uncited heuristic (rule 4). New comparator is grepai hybrid.go L57-L89 -> retrieve.go `return results[i].Score > results[j].Score` |
| FOUND | None:None | internal/store/store.go:135-137 ExactSQL comment now describes plain unicode61 without porter stemming | Comment-only change; matches ccrider schema.go L86-L122 code index tokenizer `tokenize='unicode61'` |

### run 5 @ 05:54:47 local — verdict REJECT, 19 findings

| class | file:line | decision | reason / upstream_quote |
|---|---|---|---|
| POLICY | None:None | internal/index/consolidated.go:1464 SettleWindow = 60s labeled as measured operational constant | // policy: 2026-09-04 60s settle see docs/design/decision-references.md#D10 label on an operational timeout  |
| POLICY | None:None | internal/store/store.go:345 ROMmapSize = 0x7fff0000 labeled | // policy: 2026-09-04 mmap 0x7fff0000 = SQLite ceiling see docs/design/decision-references.md#D2  |
| FOUND | None:None | internal/query/query.go:33-49 deletion of local regex sanitizer heuristics (reFTS5Structural, reLeadStar, reRunStar, reLeadBool, reTrailBool, reDottedID, reProt | Deletion of our own uncited heuristics; doctrine rule 4 losers get deleted. Retained sanitizer is the cited zk-org/zk ConvertQuery. `func ConvertQuery(query string) string {` |
| FOUND | None:None | internal/query/query.go:149-188 deletion of EscapeFTS5Query (ccrider search.go port) in favour of zk ConvertQuery as the single FTS5 escaper | The deleted function's upstream (ccrider search.go) is not a cited trailer in this range; the surviving escaper traces to zk-org/zk fts5.go L6-L104 which quotes each term `out.WriteString(`"` + term + `"`)` |
| FOUND | None:None | internal/store/fts.go:234-246 and :340-351 window RANK() OVER (ORDER BY bm25_score) computed over an inner subquery that applies WHERE/ORDER/LIMIT first | paradedb reciprocal_rank_fusion.sql L32-L60 -> fts.go: RANK() window over an inner LIMITed subquery scoring rows; ORDER BY score DESC adapted to SQLite bm25() negative polarity (ascending) is language scaffolding `RANK() OVER (ORDER BY score DESC) AS rank` |
| FOUND | None:None | internal/store/fts.go:239-240 and :347-348 inner subquery selects bm25(tbl) AS bm25_score as the rank expression | openclaw/clickclack search_pages.go L56-L75 -> fts.go uses bm25(messages_fts) as the rank expression; paradedb selects pdb.score(order_id) AS score inside the subquery `rankExpression := "bm25(messages_fts)"` |
| FOUND | None:None | internal/store/fts.go:241-243 and :349-351 inner subquery ordered by orderClause(s) with LIMIT (and OFFSET) applied before ranking | paradedb L32-L60 -> fts.go: inner SELECT ... ORDER BY pdb.score(order_id) DESC LIMIT 20 before the RANK window `SELECT order_id, pdb.score(order_id) AS score` |
| FOUND | None:None | internal/store/fts.go:243 and :351 outer query re-applies orderClause(s) after the window | paradedb L49-L68 final SELECT orders the ranked output; wacli L99-L105 -> fts.go orders by bm25 then rowid `ORDER BY score DESC, o.order_id` |
| FOUND | None:None | internal/store/fts.go SearchHit/SearchAnchor gain ID, BM25, Rank fields and scan them | Struct field assignment and scan plumbing of the cited paradedb rank/score columns; wiring per doctrine `RANK() OVER (ORDER BY score DESC) AS rank` |
| FOUND | None:None | internal/retrieve/retrieve.go:411 and :460 RRF score term 1/(k + Rank) using the SQL RANK() value instead of list position, k=60 | paradedb L49-L68 -> retrieve.go computes reciprocal of (60 + rank) from the window rank `1.0 / (60 + rank) AS s` |
| FOUND | None:None | internal/retrieve/retrieve.go:412 and :461 summing RRF contributions across lists into a per-key score map | grepai hybrid.go L57-L89 -> retrieve.go accumulates scores[id] across lists; paradedb sum(rrf.s) AS score `scores[id] += 1.0 / (k + float32(rank) + 1)` |
| FOUND | None:None | internal/retrieve/retrieve.go:415-427 and :464-476 results assembled by first-seen list order with a seen map instead of map iteration | Ordered slice beside a map is Go scaffolding per the gate procedure; the dedup-by-id itself is grepai's chunkMap dedup `Results are deduplicated by chunk ID and sorted by combined RRF score.` |
| FOUND | None:None | internal/retrieve/retrieve.go:441-445 and :482-487 sort by fused score DESC then message ID ASC; ISO DESC tie-break removed | paradedb L49-L68 -> retrieve.go final ORDER BY score DESC, o.order_id; the removed ISO tie-break was our own uncited invention (rule 4) `ORDER BY score DESC, o.order_id` |
| FOUND | None:None | internal/retrieve/retrieve.go:447 and :488 truncate fused results to limit when limit > 0 | grepai hybrid.go L57-L89 -> retrieve.go `if limit > 0 && len(results) > limit {` |
| FOUND | None:None | internal/retrieve/retrieve.go:404 and :455 rrfAnchors/rrfHits return the scores map as a second value | Function-return pass-through of the grepai score map; wiring `scores := make(map[string]float32)` |
| MADE | None:None | internal/retrieve/retrieve.go:555-571 and :853-872 new mode branch: when p.Sort is "newest" or "oldest", skip RRF and instead concatenate exact list then stemme | No cited source contains this branch. meilisearch hybrid.rs L51-L57 is a fragment comparing ScoreValue::Sort pairs and says nothing about bypassing fusion under an explicit sort. clickclack L56-L75 selects cursor predica  |
| MADE | None:None | internal/retrieve/retrieve.go:592-596 and :893-900 fused defaults to 0 when no fusion ran (hitScores/anchorScores nil), so the relevance re-rank becomes a stabl | A fallback the sources lack: grepai sorts only lists that were fused and every entry has a score; no cited line assigns a zero score to unfused hits or sorts an unfused list by a zero-valued fused key while relying on st  |
| FOUND | None:None | internal/retrieve/retrieve.go:614-618 and :917-921 replacement of the coverage/routine re-rank with sort by fused score DESC (relevance mode) | Deleted coverage/routine comparator was our own uncited heuristic (rule 4). New comparator is grepai hybrid.go L57-L89 -> retrieve.go `return results[i].Score > results[j].Score` |
| FOUND | None:None | internal/store/store.go:135-137 ExactSQL comment now describes plain unicode61 without porter stemming | Comment-only change; matches ccrider schema.go L86-L122 code index tokenizer `tokenize='unicode61'` |

### run 6 @ 05:54:48 local — verdict REJECT, 19 findings

| class | file:line | decision | reason / upstream_quote |
|---|---|---|---|
| POLICY | None:None | internal/index/consolidated.go:1464 SettleWindow = 60s labeled as measured operational constant | // policy: 2026-09-04 60s settle see docs/design/decision-references.md#D10 label on an operational timeout  |
| POLICY | None:None | internal/store/store.go:345 ROMmapSize = 0x7fff0000 labeled | // policy: 2026-09-04 mmap 0x7fff0000 = SQLite ceiling see docs/design/decision-references.md#D2  |
| FOUND | None:None | internal/query/query.go:33-49 deletion of local regex sanitizer heuristics (reFTS5Structural, reLeadStar, reRunStar, reLeadBool, reTrailBool, reDottedID, reProt | Deletion of our own uncited heuristics; doctrine rule 4 losers get deleted. Retained sanitizer is the cited zk-org/zk ConvertQuery. `func ConvertQuery(query string) string {` |
| FOUND | None:None | internal/query/query.go:149-188 deletion of EscapeFTS5Query (ccrider search.go port) in favour of zk ConvertQuery as the single FTS5 escaper | The deleted function's upstream (ccrider search.go) is not a cited trailer in this range; the surviving escaper traces to zk-org/zk fts5.go L6-L104 which quotes each term `out.WriteString(`"` + term + `"`)` |
| FOUND | None:None | internal/store/fts.go:234-246 and :340-351 window RANK() OVER (ORDER BY bm25_score) computed over an inner subquery that applies WHERE/ORDER/LIMIT first | paradedb reciprocal_rank_fusion.sql L32-L60 -> fts.go: RANK() window over an inner LIMITed subquery scoring rows; ORDER BY score DESC adapted to SQLite bm25() negative polarity (ascending) is language scaffolding `RANK() OVER (ORDER BY score DESC) AS rank` |
| FOUND | None:None | internal/store/fts.go:239-240 and :347-348 inner subquery selects bm25(tbl) AS bm25_score as the rank expression | openclaw/clickclack search_pages.go L56-L75 -> fts.go uses bm25(messages_fts) as the rank expression; paradedb selects pdb.score(order_id) AS score inside the subquery `rankExpression := "bm25(messages_fts)"` |
| FOUND | None:None | internal/store/fts.go:241-243 and :349-351 inner subquery ordered by orderClause(s) with LIMIT (and OFFSET) applied before ranking | paradedb L32-L60 -> fts.go: inner SELECT ... ORDER BY pdb.score(order_id) DESC LIMIT 20 before the RANK window `SELECT order_id, pdb.score(order_id) AS score` |
| FOUND | None:None | internal/store/fts.go:243 and :351 outer query re-applies orderClause(s) after the window | paradedb L49-L68 final SELECT orders the ranked output; wacli L99-L105 -> fts.go orders by bm25 then rowid `ORDER BY score DESC, o.order_id` |
| FOUND | None:None | internal/store/fts.go SearchHit/SearchAnchor gain ID, BM25, Rank fields and scan them | Struct field assignment and scan plumbing of the cited paradedb rank/score columns; wiring per doctrine `RANK() OVER (ORDER BY score DESC) AS rank` |
| FOUND | None:None | internal/retrieve/retrieve.go:411 and :460 RRF score term 1/(k + Rank) using the SQL RANK() value instead of list position, k=60 | paradedb L49-L68 -> retrieve.go computes reciprocal of (60 + rank) from the window rank `1.0 / (60 + rank) AS s` |
| FOUND | None:None | internal/retrieve/retrieve.go:412 and :461 summing RRF contributions across lists into a per-key score map | grepai hybrid.go L57-L89 -> retrieve.go accumulates scores[id] across lists; paradedb sum(rrf.s) AS score `scores[id] += 1.0 / (k + float32(rank) + 1)` |
| FOUND | None:None | internal/retrieve/retrieve.go:415-427 and :464-476 results assembled by first-seen list order with a seen map instead of map iteration | Ordered slice beside a map is Go scaffolding per the gate procedure; the dedup-by-id itself is grepai's chunkMap dedup `Results are deduplicated by chunk ID and sorted by combined RRF score.` |
| FOUND | None:None | internal/retrieve/retrieve.go:441-445 and :482-487 sort by fused score DESC then message ID ASC; ISO DESC tie-break removed | paradedb L49-L68 -> retrieve.go final ORDER BY score DESC, o.order_id; the removed ISO tie-break was our own uncited invention (rule 4) `ORDER BY score DESC, o.order_id` |
| FOUND | None:None | internal/retrieve/retrieve.go:447 and :488 truncate fused results to limit when limit > 0 | grepai hybrid.go L57-L89 -> retrieve.go `if limit > 0 && len(results) > limit {` |
| FOUND | None:None | internal/retrieve/retrieve.go:404 and :455 rrfAnchors/rrfHits return the scores map as a second value | Function-return pass-through of the grepai score map; wiring `scores := make(map[string]float32)` |
| MADE | None:None | internal/retrieve/retrieve.go:555-571 and :853-872 new mode branch: when p.Sort is "newest" or "oldest", skip RRF and instead concatenate exact list then stemme | No cited source contains this branch. meilisearch hybrid.rs L51-L57 is a fragment comparing ScoreValue::Sort pairs and says nothing about bypassing fusion under an explicit sort. clickclack L56-L75 selects cursor predica  |
| MADE | None:None | internal/retrieve/retrieve.go:592-596 and :893-900 fused defaults to 0 when no fusion ran (hitScores/anchorScores nil), so the relevance re-rank becomes a stabl | A fallback the sources lack: grepai sorts only lists that were fused and every entry has a score; no cited line assigns a zero score to unfused hits or sorts an unfused list by a zero-valued fused key while relying on st  |
| FOUND | None:None | internal/retrieve/retrieve.go:614-618 and :917-921 replacement of the coverage/routine re-rank with sort by fused score DESC (relevance mode) | Deleted coverage/routine comparator was our own uncited heuristic (rule 4). New comparator is grepai hybrid.go L57-L89 -> retrieve.go `return results[i].Score > results[j].Score` |
| FOUND | None:None | internal/store/store.go:135-137 ExactSQL comment now describes plain unicode61 without porter stemming | Comment-only change; matches ccrider schema.go L86-L122 code index tokenizer `tokenize='unicode61'` |

### run 7 @ 05:54:50 local — verdict REJECT, 19 findings

| class | file:line | decision | reason / upstream_quote |
|---|---|---|---|
| POLICY | None:None | internal/index/consolidated.go:1464 SettleWindow = 60s labeled as measured operational constant | // policy: 2026-09-04 60s settle see docs/design/decision-references.md#D10 label on an operational timeout  |
| POLICY | None:None | internal/store/store.go:345 ROMmapSize = 0x7fff0000 labeled | // policy: 2026-09-04 mmap 0x7fff0000 = SQLite ceiling see docs/design/decision-references.md#D2  |
| FOUND | None:None | internal/query/query.go:33-49 deletion of local regex sanitizer heuristics (reFTS5Structural, reLeadStar, reRunStar, reLeadBool, reTrailBool, reDottedID, reProt | Deletion of our own uncited heuristics; doctrine rule 4 losers get deleted. Retained sanitizer is the cited zk-org/zk ConvertQuery. `func ConvertQuery(query string) string {` |
| FOUND | None:None | internal/query/query.go:149-188 deletion of EscapeFTS5Query (ccrider search.go port) in favour of zk ConvertQuery as the single FTS5 escaper | The deleted function's upstream (ccrider search.go) is not a cited trailer in this range; the surviving escaper traces to zk-org/zk fts5.go L6-L104 which quotes each term `out.WriteString(`"` + term + `"`)` |
| FOUND | None:None | internal/store/fts.go:234-246 and :340-351 window RANK() OVER (ORDER BY bm25_score) computed over an inner subquery that applies WHERE/ORDER/LIMIT first | paradedb reciprocal_rank_fusion.sql L32-L60 -> fts.go: RANK() window over an inner LIMITed subquery scoring rows; ORDER BY score DESC adapted to SQLite bm25() negative polarity (ascending) is language scaffolding `RANK() OVER (ORDER BY score DESC) AS rank` |
| FOUND | None:None | internal/store/fts.go:239-240 and :347-348 inner subquery selects bm25(tbl) AS bm25_score as the rank expression | openclaw/clickclack search_pages.go L56-L75 -> fts.go uses bm25(messages_fts) as the rank expression; paradedb selects pdb.score(order_id) AS score inside the subquery `rankExpression := "bm25(messages_fts)"` |
| FOUND | None:None | internal/store/fts.go:241-243 and :349-351 inner subquery ordered by orderClause(s) with LIMIT (and OFFSET) applied before ranking | paradedb L32-L60 -> fts.go: inner SELECT ... ORDER BY pdb.score(order_id) DESC LIMIT 20 before the RANK window `SELECT order_id, pdb.score(order_id) AS score` |
| FOUND | None:None | internal/store/fts.go:243 and :351 outer query re-applies orderClause(s) after the window | paradedb L49-L68 final SELECT orders the ranked output; wacli L99-L105 -> fts.go orders by bm25 then rowid `ORDER BY score DESC, o.order_id` |
| FOUND | None:None | internal/store/fts.go SearchHit/SearchAnchor gain ID, BM25, Rank fields and scan them | Struct field assignment and scan plumbing of the cited paradedb rank/score columns; wiring per doctrine `RANK() OVER (ORDER BY score DESC) AS rank` |
| FOUND | None:None | internal/retrieve/retrieve.go:411 and :460 RRF score term 1/(k + Rank) using the SQL RANK() value instead of list position, k=60 | paradedb L49-L68 -> retrieve.go computes reciprocal of (60 + rank) from the window rank `1.0 / (60 + rank) AS s` |
| FOUND | None:None | internal/retrieve/retrieve.go:412 and :461 summing RRF contributions across lists into a per-key score map | grepai hybrid.go L57-L89 -> retrieve.go accumulates scores[id] across lists; paradedb sum(rrf.s) AS score `scores[id] += 1.0 / (k + float32(rank) + 1)` |
| FOUND | None:None | internal/retrieve/retrieve.go:415-427 and :464-476 results assembled by first-seen list order with a seen map instead of map iteration | Ordered slice beside a map is Go scaffolding per the gate procedure; the dedup-by-id itself is grepai's chunkMap dedup `Results are deduplicated by chunk ID and sorted by combined RRF score.` |
| FOUND | None:None | internal/retrieve/retrieve.go:441-445 and :482-487 sort by fused score DESC then message ID ASC; ISO DESC tie-break removed | paradedb L49-L68 -> retrieve.go final ORDER BY score DESC, o.order_id; the removed ISO tie-break was our own uncited invention (rule 4) `ORDER BY score DESC, o.order_id` |
| FOUND | None:None | internal/retrieve/retrieve.go:447 and :488 truncate fused results to limit when limit > 0 | grepai hybrid.go L57-L89 -> retrieve.go `if limit > 0 && len(results) > limit {` |
| FOUND | None:None | internal/retrieve/retrieve.go:404 and :455 rrfAnchors/rrfHits return the scores map as a second value | Function-return pass-through of the grepai score map; wiring `scores := make(map[string]float32)` |
| MADE | None:None | internal/retrieve/retrieve.go:555-571 and :853-872 new mode branch: when p.Sort is "newest" or "oldest", skip RRF and instead concatenate exact list then stemme | No cited source contains this branch. meilisearch hybrid.rs L51-L57 is a fragment comparing ScoreValue::Sort pairs and says nothing about bypassing fusion under an explicit sort. clickclack L56-L75 selects cursor predica  |
| MADE | None:None | internal/retrieve/retrieve.go:592-596 and :893-900 fused defaults to 0 when no fusion ran (hitScores/anchorScores nil), so the relevance re-rank becomes a stabl | A fallback the sources lack: grepai sorts only lists that were fused and every entry has a score; no cited line assigns a zero score to unfused hits or sorts an unfused list by a zero-valued fused key while relying on st  |
| FOUND | None:None | internal/retrieve/retrieve.go:614-618 and :917-921 replacement of the coverage/routine re-rank with sort by fused score DESC (relevance mode) | Deleted coverage/routine comparator was our own uncited heuristic (rule 4). New comparator is grepai hybrid.go L57-L89 -> retrieve.go `return results[i].Score > results[j].Score` |
| FOUND | None:None | internal/store/store.go:135-137 ExactSQL comment now describes plain unicode61 without porter stemming | Comment-only change; matches ccrider schema.go L86-L122 code index tokenizer `tokenize='unicode61'` |

### run 8 @ 05:54:51 local — verdict REJECT, 19 findings

| class | file:line | decision | reason / upstream_quote |
|---|---|---|---|
| POLICY | None:None | internal/index/consolidated.go:1464 SettleWindow = 60s labeled as measured operational constant | // policy: 2026-09-04 60s settle see docs/design/decision-references.md#D10 label on an operational timeout  |
| POLICY | None:None | internal/store/store.go:345 ROMmapSize = 0x7fff0000 labeled | // policy: 2026-09-04 mmap 0x7fff0000 = SQLite ceiling see docs/design/decision-references.md#D2  |
| FOUND | None:None | internal/query/query.go:33-49 deletion of local regex sanitizer heuristics (reFTS5Structural, reLeadStar, reRunStar, reLeadBool, reTrailBool, reDottedID, reProt | Deletion of our own uncited heuristics; doctrine rule 4 losers get deleted. Retained sanitizer is the cited zk-org/zk ConvertQuery. `func ConvertQuery(query string) string {` |
| FOUND | None:None | internal/query/query.go:149-188 deletion of EscapeFTS5Query (ccrider search.go port) in favour of zk ConvertQuery as the single FTS5 escaper | The deleted function's upstream (ccrider search.go) is not a cited trailer in this range; the surviving escaper traces to zk-org/zk fts5.go L6-L104 which quotes each term `out.WriteString(`"` + term + `"`)` |
| FOUND | None:None | internal/store/fts.go:234-246 and :340-351 window RANK() OVER (ORDER BY bm25_score) computed over an inner subquery that applies WHERE/ORDER/LIMIT first | paradedb reciprocal_rank_fusion.sql L32-L60 -> fts.go: RANK() window over an inner LIMITed subquery scoring rows; ORDER BY score DESC adapted to SQLite bm25() negative polarity (ascending) is language scaffolding `RANK() OVER (ORDER BY score DESC) AS rank` |
| FOUND | None:None | internal/store/fts.go:239-240 and :347-348 inner subquery selects bm25(tbl) AS bm25_score as the rank expression | openclaw/clickclack search_pages.go L56-L75 -> fts.go uses bm25(messages_fts) as the rank expression; paradedb selects pdb.score(order_id) AS score inside the subquery `rankExpression := "bm25(messages_fts)"` |
| FOUND | None:None | internal/store/fts.go:241-243 and :349-351 inner subquery ordered by orderClause(s) with LIMIT (and OFFSET) applied before ranking | paradedb L32-L60 -> fts.go: inner SELECT ... ORDER BY pdb.score(order_id) DESC LIMIT 20 before the RANK window `SELECT order_id, pdb.score(order_id) AS score` |
| FOUND | None:None | internal/store/fts.go:243 and :351 outer query re-applies orderClause(s) after the window | paradedb L49-L68 final SELECT orders the ranked output; wacli L99-L105 -> fts.go orders by bm25 then rowid `ORDER BY score DESC, o.order_id` |
| FOUND | None:None | internal/store/fts.go SearchHit/SearchAnchor gain ID, BM25, Rank fields and scan them | Struct field assignment and scan plumbing of the cited paradedb rank/score columns; wiring per doctrine `RANK() OVER (ORDER BY score DESC) AS rank` |
| FOUND | None:None | internal/retrieve/retrieve.go:411 and :460 RRF score term 1/(k + Rank) using the SQL RANK() value instead of list position, k=60 | paradedb L49-L68 -> retrieve.go computes reciprocal of (60 + rank) from the window rank `1.0 / (60 + rank) AS s` |
| FOUND | None:None | internal/retrieve/retrieve.go:412 and :461 summing RRF contributions across lists into a per-key score map | grepai hybrid.go L57-L89 -> retrieve.go accumulates scores[id] across lists; paradedb sum(rrf.s) AS score `scores[id] += 1.0 / (k + float32(rank) + 1)` |
| FOUND | None:None | internal/retrieve/retrieve.go:415-427 and :464-476 results assembled by first-seen list order with a seen map instead of map iteration | Ordered slice beside a map is Go scaffolding per the gate procedure; the dedup-by-id itself is grepai's chunkMap dedup `Results are deduplicated by chunk ID and sorted by combined RRF score.` |
| FOUND | None:None | internal/retrieve/retrieve.go:441-445 and :482-487 sort by fused score DESC then message ID ASC; ISO DESC tie-break removed | paradedb L49-L68 -> retrieve.go final ORDER BY score DESC, o.order_id; the removed ISO tie-break was our own uncited invention (rule 4) `ORDER BY score DESC, o.order_id` |
| FOUND | None:None | internal/retrieve/retrieve.go:447 and :488 truncate fused results to limit when limit > 0 | grepai hybrid.go L57-L89 -> retrieve.go `if limit > 0 && len(results) > limit {` |
| FOUND | None:None | internal/retrieve/retrieve.go:404 and :455 rrfAnchors/rrfHits return the scores map as a second value | Function-return pass-through of the grepai score map; wiring `scores := make(map[string]float32)` |
| MADE | None:None | internal/retrieve/retrieve.go:555-571 and :853-872 new mode branch: when p.Sort is "newest" or "oldest", skip RRF and instead concatenate exact list then stemme | No cited source contains this branch. meilisearch hybrid.rs L51-L57 is a fragment comparing ScoreValue::Sort pairs and says nothing about bypassing fusion under an explicit sort. clickclack L56-L75 selects cursor predica  |
| MADE | None:None | internal/retrieve/retrieve.go:592-596 and :893-900 fused defaults to 0 when no fusion ran (hitScores/anchorScores nil), so the relevance re-rank becomes a stabl | A fallback the sources lack: grepai sorts only lists that were fused and every entry has a score; no cited line assigns a zero score to unfused hits or sorts an unfused list by a zero-valued fused key while relying on st  |
| FOUND | None:None | internal/retrieve/retrieve.go:614-618 and :917-921 replacement of the coverage/routine re-rank with sort by fused score DESC (relevance mode) | Deleted coverage/routine comparator was our own uncited heuristic (rule 4). New comparator is grepai hybrid.go L57-L89 -> retrieve.go `return results[i].Score > results[j].Score` |
| FOUND | None:None | internal/store/store.go:135-137 ExactSQL comment now describes plain unicode61 without porter stemming | Comment-only change; matches ccrider schema.go L86-L122 code index tokenizer `tokenize='unicode61'` |

### run 9 @ 05:54:52 local — verdict REJECT, 19 findings

| class | file:line | decision | reason / upstream_quote |
|---|---|---|---|
| POLICY | None:None | internal/index/consolidated.go:1464 SettleWindow = 60s labeled as measured operational constant | // policy: 2026-09-04 60s settle see docs/design/decision-references.md#D10 label on an operational timeout  |
| POLICY | None:None | internal/store/store.go:345 ROMmapSize = 0x7fff0000 labeled | // policy: 2026-09-04 mmap 0x7fff0000 = SQLite ceiling see docs/design/decision-references.md#D2  |
| FOUND | None:None | internal/query/query.go:33-49 deletion of local regex sanitizer heuristics (reFTS5Structural, reLeadStar, reRunStar, reLeadBool, reTrailBool, reDottedID, reProt | Deletion of our own uncited heuristics; doctrine rule 4 losers get deleted. Retained sanitizer is the cited zk-org/zk ConvertQuery. `func ConvertQuery(query string) string {` |
| FOUND | None:None | internal/query/query.go:149-188 deletion of EscapeFTS5Query (ccrider search.go port) in favour of zk ConvertQuery as the single FTS5 escaper | The deleted function's upstream (ccrider search.go) is not a cited trailer in this range; the surviving escaper traces to zk-org/zk fts5.go L6-L104 which quotes each term `out.WriteString(`"` + term + `"`)` |
| FOUND | None:None | internal/store/fts.go:234-246 and :340-351 window RANK() OVER (ORDER BY bm25_score) computed over an inner subquery that applies WHERE/ORDER/LIMIT first | paradedb reciprocal_rank_fusion.sql L32-L60 -> fts.go: RANK() window over an inner LIMITed subquery scoring rows; ORDER BY score DESC adapted to SQLite bm25() negative polarity (ascending) is language scaffolding `RANK() OVER (ORDER BY score DESC) AS rank` |
| FOUND | None:None | internal/store/fts.go:239-240 and :347-348 inner subquery selects bm25(tbl) AS bm25_score as the rank expression | openclaw/clickclack search_pages.go L56-L75 -> fts.go uses bm25(messages_fts) as the rank expression; paradedb selects pdb.score(order_id) AS score inside the subquery `rankExpression := "bm25(messages_fts)"` |
| FOUND | None:None | internal/store/fts.go:241-243 and :349-351 inner subquery ordered by orderClause(s) with LIMIT (and OFFSET) applied before ranking | paradedb L32-L60 -> fts.go: inner SELECT ... ORDER BY pdb.score(order_id) DESC LIMIT 20 before the RANK window `SELECT order_id, pdb.score(order_id) AS score` |
| FOUND | None:None | internal/store/fts.go:243 and :351 outer query re-applies orderClause(s) after the window | paradedb L49-L68 final SELECT orders the ranked output; wacli L99-L105 -> fts.go orders by bm25 then rowid `ORDER BY score DESC, o.order_id` |
| FOUND | None:None | internal/store/fts.go SearchHit/SearchAnchor gain ID, BM25, Rank fields and scan them | Struct field assignment and scan plumbing of the cited paradedb rank/score columns; wiring per doctrine `RANK() OVER (ORDER BY score DESC) AS rank` |
| FOUND | None:None | internal/retrieve/retrieve.go:411 and :460 RRF score term 1/(k + Rank) using the SQL RANK() value instead of list position, k=60 | paradedb L49-L68 -> retrieve.go computes reciprocal of (60 + rank) from the window rank `1.0 / (60 + rank) AS s` |
| FOUND | None:None | internal/retrieve/retrieve.go:412 and :461 summing RRF contributions across lists into a per-key score map | grepai hybrid.go L57-L89 -> retrieve.go accumulates scores[id] across lists; paradedb sum(rrf.s) AS score `scores[id] += 1.0 / (k + float32(rank) + 1)` |
| FOUND | None:None | internal/retrieve/retrieve.go:415-427 and :464-476 results assembled by first-seen list order with a seen map instead of map iteration | Ordered slice beside a map is Go scaffolding per the gate procedure; the dedup-by-id itself is grepai's chunkMap dedup `Results are deduplicated by chunk ID and sorted by combined RRF score.` |
| FOUND | None:None | internal/retrieve/retrieve.go:441-445 and :482-487 sort by fused score DESC then message ID ASC; ISO DESC tie-break removed | paradedb L49-L68 -> retrieve.go final ORDER BY score DESC, o.order_id; the removed ISO tie-break was our own uncited invention (rule 4) `ORDER BY score DESC, o.order_id` |
| FOUND | None:None | internal/retrieve/retrieve.go:447 and :488 truncate fused results to limit when limit > 0 | grepai hybrid.go L57-L89 -> retrieve.go `if limit > 0 && len(results) > limit {` |
| FOUND | None:None | internal/retrieve/retrieve.go:404 and :455 rrfAnchors/rrfHits return the scores map as a second value | Function-return pass-through of the grepai score map; wiring `scores := make(map[string]float32)` |
| MADE | None:None | internal/retrieve/retrieve.go:555-571 and :853-872 new mode branch: when p.Sort is "newest" or "oldest", skip RRF and instead concatenate exact list then stemme | No cited source contains this branch. meilisearch hybrid.rs L51-L57 is a fragment comparing ScoreValue::Sort pairs and says nothing about bypassing fusion under an explicit sort. clickclack L56-L75 selects cursor predica  |
| MADE | None:None | internal/retrieve/retrieve.go:592-596 and :893-900 fused defaults to 0 when no fusion ran (hitScores/anchorScores nil), so the relevance re-rank becomes a stabl | A fallback the sources lack: grepai sorts only lists that were fused and every entry has a score; no cited line assigns a zero score to unfused hits or sorts an unfused list by a zero-valued fused key while relying on st  |
| FOUND | None:None | internal/retrieve/retrieve.go:614-618 and :917-921 replacement of the coverage/routine re-rank with sort by fused score DESC (relevance mode) | Deleted coverage/routine comparator was our own uncited heuristic (rule 4). New comparator is grepai hybrid.go L57-L89 -> retrieve.go `return results[i].Score > results[j].Score` |
| FOUND | None:None | internal/store/store.go:135-137 ExactSQL comment now describes plain unicode61 without porter stemming | Comment-only change; matches ccrider schema.go L86-L122 code index tokenizer `tokenize='unicode61'` |

### run 10 @ 05:54:54 local — verdict REJECT, 19 findings

| class | file:line | decision | reason / upstream_quote |
|---|---|---|---|
| POLICY | None:None | internal/index/consolidated.go:1464 SettleWindow = 60s labeled as measured operational constant | // policy: 2026-09-04 60s settle see docs/design/decision-references.md#D10 label on an operational timeout  |
| POLICY | None:None | internal/store/store.go:345 ROMmapSize = 0x7fff0000 labeled | // policy: 2026-09-04 mmap 0x7fff0000 = SQLite ceiling see docs/design/decision-references.md#D2  |
| FOUND | None:None | internal/query/query.go:33-49 deletion of local regex sanitizer heuristics (reFTS5Structural, reLeadStar, reRunStar, reLeadBool, reTrailBool, reDottedID, reProt | Deletion of our own uncited heuristics; doctrine rule 4 losers get deleted. Retained sanitizer is the cited zk-org/zk ConvertQuery. `func ConvertQuery(query string) string {` |
| FOUND | None:None | internal/query/query.go:149-188 deletion of EscapeFTS5Query (ccrider search.go port) in favour of zk ConvertQuery as the single FTS5 escaper | The deleted function's upstream (ccrider search.go) is not a cited trailer in this range; the surviving escaper traces to zk-org/zk fts5.go L6-L104 which quotes each term `out.WriteString(`"` + term + `"`)` |
| FOUND | None:None | internal/store/fts.go:234-246 and :340-351 window RANK() OVER (ORDER BY bm25_score) computed over an inner subquery that applies WHERE/ORDER/LIMIT first | paradedb reciprocal_rank_fusion.sql L32-L60 -> fts.go: RANK() window over an inner LIMITed subquery scoring rows; ORDER BY score DESC adapted to SQLite bm25() negative polarity (ascending) is language scaffolding `RANK() OVER (ORDER BY score DESC) AS rank` |
| FOUND | None:None | internal/store/fts.go:239-240 and :347-348 inner subquery selects bm25(tbl) AS bm25_score as the rank expression | openclaw/clickclack search_pages.go L56-L75 -> fts.go uses bm25(messages_fts) as the rank expression; paradedb selects pdb.score(order_id) AS score inside the subquery `rankExpression := "bm25(messages_fts)"` |
| FOUND | None:None | internal/store/fts.go:241-243 and :349-351 inner subquery ordered by orderClause(s) with LIMIT (and OFFSET) applied before ranking | paradedb L32-L60 -> fts.go: inner SELECT ... ORDER BY pdb.score(order_id) DESC LIMIT 20 before the RANK window `SELECT order_id, pdb.score(order_id) AS score` |
| FOUND | None:None | internal/store/fts.go:243 and :351 outer query re-applies orderClause(s) after the window | paradedb L49-L68 final SELECT orders the ranked output; wacli L99-L105 -> fts.go orders by bm25 then rowid `ORDER BY score DESC, o.order_id` |
| FOUND | None:None | internal/store/fts.go SearchHit/SearchAnchor gain ID, BM25, Rank fields and scan them | Struct field assignment and scan plumbing of the cited paradedb rank/score columns; wiring per doctrine `RANK() OVER (ORDER BY score DESC) AS rank` |
| FOUND | None:None | internal/retrieve/retrieve.go:411 and :460 RRF score term 1/(k + Rank) using the SQL RANK() value instead of list position, k=60 | paradedb L49-L68 -> retrieve.go computes reciprocal of (60 + rank) from the window rank `1.0 / (60 + rank) AS s` |
| FOUND | None:None | internal/retrieve/retrieve.go:412 and :461 summing RRF contributions across lists into a per-key score map | grepai hybrid.go L57-L89 -> retrieve.go accumulates scores[id] across lists; paradedb sum(rrf.s) AS score `scores[id] += 1.0 / (k + float32(rank) + 1)` |
| FOUND | None:None | internal/retrieve/retrieve.go:415-427 and :464-476 results assembled by first-seen list order with a seen map instead of map iteration | Ordered slice beside a map is Go scaffolding per the gate procedure; the dedup-by-id itself is grepai's chunkMap dedup `Results are deduplicated by chunk ID and sorted by combined RRF score.` |
| FOUND | None:None | internal/retrieve/retrieve.go:441-445 and :482-487 sort by fused score DESC then message ID ASC; ISO DESC tie-break removed | paradedb L49-L68 -> retrieve.go final ORDER BY score DESC, o.order_id; the removed ISO tie-break was our own uncited invention (rule 4) `ORDER BY score DESC, o.order_id` |
| FOUND | None:None | internal/retrieve/retrieve.go:447 and :488 truncate fused results to limit when limit > 0 | grepai hybrid.go L57-L89 -> retrieve.go `if limit > 0 && len(results) > limit {` |
| FOUND | None:None | internal/retrieve/retrieve.go:404 and :455 rrfAnchors/rrfHits return the scores map as a second value | Function-return pass-through of the grepai score map; wiring `scores := make(map[string]float32)` |
| MADE | None:None | internal/retrieve/retrieve.go:555-571 and :853-872 new mode branch: when p.Sort is "newest" or "oldest", skip RRF and instead concatenate exact list then stemme | No cited source contains this branch. meilisearch hybrid.rs L51-L57 is a fragment comparing ScoreValue::Sort pairs and says nothing about bypassing fusion under an explicit sort. clickclack L56-L75 selects cursor predica  |
| MADE | None:None | internal/retrieve/retrieve.go:592-596 and :893-900 fused defaults to 0 when no fusion ran (hitScores/anchorScores nil), so the relevance re-rank becomes a stabl | A fallback the sources lack: grepai sorts only lists that were fused and every entry has a score; no cited line assigns a zero score to unfused hits or sorts an unfused list by a zero-valued fused key while relying on st  |
| FOUND | None:None | internal/retrieve/retrieve.go:614-618 and :917-921 replacement of the coverage/routine re-rank with sort by fused score DESC (relevance mode) | Deleted coverage/routine comparator was our own uncited heuristic (rule 4). New comparator is grepai hybrid.go L57-L89 -> retrieve.go `return results[i].Score > results[j].Score` |
| FOUND | None:None | internal/store/store.go:135-137 ExactSQL comment now describes plain unicode61 without porter stemming | Comment-only change; matches ccrider schema.go L86-L122 code index tokenizer `tokenize='unicode61'` |

## gemini-subscription (gemini-3.7-flash-high)

10 runs, gate recorded APPROVE 10/10. Raw responses were NOT captured (the capture hook was added after this run). The gate log for those runs shows zero MADE lines and zero findings printed. Whether the model returned an empty findings array or the gate failed to parse a findings array is unknown from the surviving evidence. Rerun required with capture.

## Determinism check on claude-sub/claude-fable-5-1 (2026-09-08 06:35 local)

Earlier tonight I reported Fable "12/12 identical at temperature 0". That prompt was fixed text, so a gateway or provider cache could have answered. Re-run with a per-call nonce so nothing can be cached, 4 calls per temperature, prompt "Session <nonce>. Invent a two-word name for a fictional pub. Output only the name.":

```
temp 0: The Rusty Anchor / The Rusted Anchor / The Rusted Anchor / The Rusted Anvil
temp 1: The Gilded Otter / The Rusted Anchor / The Rusted Anchor / The Rusted Anvil
```
Conclusion: temperature 0 through this gateway does NOT make Fable byte-deterministic; outputs vary at both settings, with slightly less spread at 0. The 10/10 identical judge findings earlier are therefore a property of the prompt being highly constrained, not of the sampling being fixed. Same nonce test on the other two, temp 0, 4 calls each:
```
opus-5:   The Copper Wren / The Copper Wren / The Idle Compiler / The Copper Wren   (varies)
sonnet-5: The Rusty Anchor x4                                                        (no variance in this sample)
```
So: Sonnet was byte-stable on this tiny probe, Opus and Fable were not. None of this is a determinism guarantee; it is four calls. My earlier "deterministic" claim is withdrawn for all three.

Also: `antigravity-flash-fast` resolves to `openai/gemini-3-flash` (an older generation), not 3.7. Not a candidate.

## What this ledger does not establish

- No ground-truth label set: which of these findings are correct is not decided here.
- No clean-diff control was run.
- Astra's row (REJECT 5/5, 10 sites, msg 433 in fleet-doctrine) was produced on 2026-09-07 under an earlier prompt version; not the same experiment.
- Gemini 3.7 Flash LOW and MEDIUM are not exposed on the gateway for this key (400 on every alias tried); only HIGH ran. A low/high comparison needs the agy CLI path or a new gateway alias.

## Corrections from the four independent recon agents (2026-09-08 07:30 local; raw reports in `docs/design/recon-2026-09-08/`)

Four Codex workers on the gateway's Gemini 3.7 Flash route, four different briefs (hop trace / hostile audit / architecture map / low-vs-high measurement). Their outputs, cross-checked against each other and against my own direct probes:

1. **`temperature: 0` never reached any Claude 5 model.** Anthropic rejects the parameter on the 5-series (`"temperature is deprecated for this model"`, reproduced by me straight against sub2api: fable-5-1 → `invalid_request_error`; sonnet-4-6 → accepted). LiteLLM has `drop_params: true` (gateway config line 401), so it silently strips it and returns 200. Every "temp 0" Anthropic judge run in this ledger ran at the provider default. The premise of the Anthropic rows is void. (Recon A, verified.)
2. **The gateway has a Redis response cache, ttl 600 s** (config lines 405–411). Every "byte-identical N/N" claim tonight that used a fixed prompt, mine and Recon B's 12/12 and Recon D's 5/5, is explained by the cache. With `cache: {"no-cache": true}` and the same fixed prompt, Fable gave 4 different hashes in 4 calls. Recon B's VERIFIED on claim (c) is therefore wrong in the same way I was.
3. **No dated snapshot exists behind `claude-opus-5` / `claude-sonnet-5` / `claude-fable-5-1` on this lane.** Anthropic's API accepts and echoes the undated ID; the thinking-block signature encodes `claude-opus-5` literally; sub2api's catalog gives creation dates (opus-5 2026-07-25, sonnet-5 2026-07-01, fable-5-1 2026-09-01) but no date-suffixed IDs. (Recon B and C, consistent with sub2api logs.) The Anthropic account is `claude-max-tools-rh` = tools@remotelyhuman.com, not icecreamtuna.
4. **Gemini 3.7 Flash LOW and MEDIUM do not exist on cliproxy.** cliproxy's own `/v1/models` lists only `gemini-3.7-flash-high`; requests for -low/-medium get `400 unknown provider` from cliproxy itself, not from LiteLLM. (Recon A and D.) The agy CLI lists low/medium/high because it uses a different model map against the same Google account (`antigravity-icecreamtuna@gmail.com`, endpoint `daily-cloudcode-pa.googleapis.com`). The low-vs-high judge comparison can only run through the agy CLI, which is timing out on every prompt as of 07:00.
5. **Gemini 3.7 Flash high's APPROVE on the known-bad range is a real verdict, not a parse failure.** Recon D's captured response: 12 findings, all FOUND or POLICY, including `"Explicit sort modes bypass relevance score ordering"` classified FOUND. It also approved the clean control range 5/5. So on this input it distinguishes nothing.
6. **The agy CLI and cliproxy are the same subscription.** Same OAuth account, same Google endpoint. Two transports, one quota.

Net effect on this ledger: the Anthropic rows were not run at temperature 0; no row's "identical across runs" claim survives the cache; Gemini high is lenient by classification, not by malfunction; low/medium Gemini was never runnable through the gateway.
