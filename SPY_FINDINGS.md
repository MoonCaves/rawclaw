# Rival Worktree Spy Findings

Audit date: 2026-08-26. Base compared: `origin/integrate/tagwrite-closeout-wave1` (`479d14c`). Rival trees were read and tested without edits.

## Ranked findings

1. **delete — permanent catalog claim can suppress ingest forever.** SHA `7a78884c977998230c4ee14f348a883aa181d620` (`rawclaw-lenny-raid-hooks`), `internal/cli/setup.go:83-88,89-96` and Codex `:159-166,172-184`: the hook creates `$entry` with `set -C` before launching detached `rawclaw ingest`; if ingest exits or is killed before replacing the empty marker, the next SessionStart sees `-e "$entry"` and exits without retrying. The rival tests only use a stub that succeeds (`internal/cli/cmd_ingest_test.go:153-156`) and have no failed-ingest-then-retry case, so the advertised dedup gate does not prove loss-free retry. Replacement: claim only after durable ingest success, or make the marker include retryable in-progress state/age. Branch production delta is `+124/-190 = -66` lines; test delta is `+183/-4 = +179` lines. Focused gate observed: `CGO_ENABLED=0 go test -race -count=1 ./internal/cli -run 'TestPrimeScripts_SessionStartDeduplicatesConcurrentIngest|TestPrimeScripts_EmitDetachedIngest'` passed in `4.667s`; it does not cover this failure.

2. **shrink — four skill lanes duplicate one exact code/test diff.** SHAs `65f3b8b236810827275c4ca00a00327b8119796d`, `7bf86ec27e821b3ae31fb70a047ef49add5869b1`, `62095345b14124b63cd907a4abdce261a67241cb`, and `37e4f70b5490eda0e9c3e409cc4a1e163082b856` (`rawclaw-lenny-skill-{architecture,modernize,interfaces,style}`), paths `internal/agentproto/agentproto.go:...`, `internal/cli/cmd_ingest_test.go:...`, `internal/cli/cmd_tag_onestore_test.go:...`, `internal/cli/setup.go:...`, `internal/index/consolidated_fence_test.go:...` (each branch's `FINDINGS.md:1` identifies its lane). The production/test patch hashes are identical (`70e338c73384b7addaa3f4cbcfdf5da3c225c38d157c2c248ddef00d8ac3e091`) and each is `+286/-41 = +245` net lines; only the review document differs. Safe transplant: select one reviewed code patch, discard the other three lanes' duplicate code/test copies. All four worktrees were clean; no merge claim is supported by the lane separation itself.

3. **shrink — fence timeout test adds a process-global logger mutation without proving concurrent safety.** SHA `6ddd17a373114f8ca643cabe26014370e9e432a9` (`rawclaw-lenny-raid-fence`), `internal/index/consolidated_fence_test.go:84-155`: the new 72-line `TestConsolidatedFence_LogsAcquireTimeoutDuration` calls `slog.SetDefault` at `:96-99`, then restores it with `defer`; this global mutation is not isolated from other package tests and the test does not run with `t.Parallel` or synchronize the default logger. It proves ordering/duration only in a serial invocation, not race-free package behavior. The branch adds `+74` test lines and no production lines. Observed gate: `CGO_ENABLED=0 go test -race -count=1 ./internal/index -run 'TestConsolidatedFence_LogsAcquireTimeoutDuration|TestConsolidate_LogsPhaseStartsAndDurations'` passed in `3.196s`; classify the broader concurrency claim as **UNPROVEN**, not failed. Safe transplant: fold the assertion into existing recorder infrastructure or explicitly serialize global logger replacement.

## Lane status and safe opportunities

- `rawclaw-lenny-raid-phase` SHA `dd57060432ef4fafff056a37308b66cd1eba7cef`: clean, `+32/-42` production refactor; focused phase gate was not claimed as a defect. The helper is a plausible transplant only with the existing phase contract retained.
- `rawclaw-lenny-raid-locate` SHA `fc1a0759d429c43bb5cf150f77ac79f10c18d3fc`: clean, `+54/-84` production; focused tag/segment gate passed in `19.975s`. The `resolveSegmentRange` extraction is the safest small transplant.
- `rawclaw-lenny-raid-prewarm` SHA `229f7e9317d02612f39825a46f511baff42bd887`: clean, docs-only `+42/-25`; no code transplant.
- `rawclaw-lenny-raid-containers` SHA `be4ef6c30141900e170fb9cf9f557a12951477fc`: clean, `+107/-2` production/test; focused WAL/container gate passed in `2.977s`. The metadata helper is a plausible transplant, but its 99-line table test should be retained only if the contract is missing on the chosen base.
- All ten named rival worktrees were clean at inspection; no dirty/stalled worktree was observed. The `raid-fence`, `raid-hooks`, `raid-prewarm`, `raid-containers`, and `skill-interfaces/style` heartbeat labels are therefore process-age signals, not source-tree evidence of a stall.

Net judgment: one confirmed retry-suppression defect, one duplicated four-lane code payload, and one concurrency gate whose scope is unproven. Focused checks were green but do not erase those limitations.
