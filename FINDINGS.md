Thread the existing `query.PathPredicate(include, exclude)` from the scoped CLI browse/search entry points through `allScope` and `scopes.All` into Codex, Antigravity, and Goose, with empty patterns preserving current unfiltered callers; have `containerScopes` filter `byCWD` keys before sorting/indexing, while retaining the later `FilterByPath` pass for Claude, archive, and orphan scopes.
